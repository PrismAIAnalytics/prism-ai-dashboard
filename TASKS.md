# TASKS.md

> **Single source of truth for what's in flight.** This file is the AI-coordination lock — see [WORKFLOW.md](WORKFLOW.md) Section 3.
> **The "In Progress" table contains zero or one row. Never two.**

---

## In Progress

| Task ID | Title | Branch | Owner | Started |
|---------|-------|--------|-------|---------|
| T-090 | Audit-defensible ticket body content — patch [`services/inboxRouter.js`](services/inboxRouter.js) `createCapture` to write the full untruncated capture text into the Notion page body wrapped in a structured **Origin** block (`createdBy / capturedAt / sourceSurface / originalAsk`); extend [`services/notionAdapter.js`](services/notionAdapter.js) `createTicket` with an optional `body` string param that flows to `notionCreatePage` as `children` (backward-compatible — omitted body = current blank-page behavior); add a `[TRIAGE-EVENT]` greppable comment append on `triage(action='ticket')` so the promotion event is recorded on the ticket itself; amend workspace [`CLAUDE.md`](../../CLAUDE.md) `## Ticket discipline` section with new `### Required body content at creation` subsection mandating the Origin / Context / Trail structure for all tickets including agent-created ones via `mcp__notion-create-pages`; add `scripts/stamp-pre-audit-tickets.js` as a post-merge operational tool (idempotent, dry-run flag) to stamp legacy empty-body Notion tickets with a pre-standard admonition. Root cause was a comment in [`services/inboxRouter.js`](services/inboxRouter.js) lines 31-33 admitting the full capture text was meant to ride as `description` once T-040 vault writer shipped — T-040 never shipped, so every inbox capture has silently truncated to 60 chars at creation. Notion ticket [`36e236b6-b03a-818e-adbc-c5034ab69d51`](https://www.notion.so/36e236b6b03a818eadbcc5034ab69d51) (Priority: High, Category: CRM Development) — filed 2026-05-28 via cowork session `cowork:ticket-audit-standard-5-28` after Michele opened NTN-0265 / PRISM-533 (`Update two stale Apr-10 handoff docs (Development/dashboard…`) and found only a truncated title with no body / parent / plan / reason. Scope chosen via AskUserQuestion: full (capture + triage + agent playbook + backfill marker); manual sweep for legacy fix-forward. T-089 close-out bundled into this row's first commit per WORKFLOW.md §4. | feature/audit-defensible-ticket-bodies | Claude Code | 2026-05-28 |

---

## Up Next (priority order)

| ID | Title | Why it matters | Owner candidate |
|----|-------|----------------|-----------------|
| T-022 | Activate PRISM-3 — Stripe + QuickBooks live data on Financials page (env vars + QBO OAuth, no merge) | PRISM-3 (Notion) integration code shipped 2026-04-09: 3 service files, 13 endpoints, 4-tab Financials page, 6 charts, OAuth flow, in-memory cache, graceful degradation. Verified intact 2026-05-02 (services/stripeService.js, services/quickbooksService.js, server.js:5745-5961). Outstanding work is operational, not code: (1) set `STRIPE_SECRET_KEY` (sk_live_...) on Railway prod env, (2) set `QBO_CLIENT_ID`, `QBO_CLIENT_SECRET`, `QBO_REDIRECT_URI=https://dashboard-api-production-dabe.up.railway.app/api/qbo/callback`, `QBO_ENVIRONMENT=production` on Railway, (3) confirm Intuit Developer app has the Railway callback URL in production OAuth settings, (4) trigger Railway redeploy (auto on env change), (5) smoke-test `GET /api/stripe/balance` returns real numbers, (6) click "Connect QB" on Financials page, complete Intuit OAuth in browser, verify `/api/qbo/status` returns `connected: true` and `qbo_tokens` row written to SQLite. **No merge SHA — this is env + OAuth only, no code change.** **WIP=1 note:** Filed while PS-053 holds the lock; this work is no-merge so it doesn't conflict with the dashboard-repo branch lock — track it as out-of-band operational change with Closed-date entry on completion (no merge column). Followup overlap: T-013 (full Stripe/QBO secret rotation) supersedes this if rotation runs first; if T-022 runs first, T-013 still needs to rotate the same keys later. Note for future ops: 22 days of dormant Stripe + QBO capability — once activated, the Financials page becomes the single source of truth for revenue, surpassing the legacy split between Stripe dashboard and QBO. | Michele (creds + OAuth) + Claude Code (verification) |
| T-027a | Notion-as-source migration Phase 5a — remove SQLite tickets table + remove `USE_NOTION_SOURCE` flag from `/api/tickets*` routes only | Final irreversible drop of the SQLite `tickets` table. **Blocked by T-027b** (comments adapter must ship first; otherwise the `ticket_comments` FK breaks the Review-status protocol). Work: boot-time migration removes `tickets` + `ticket_comments` tables (after T-027b backfill verified); strip `USE_NOTION_SOURCE === 'true'` conditionals from `/api/tickets*` routes (Notion adapter path becomes unconditional); delete seed-tickets path at server.js:2076-2090; verify `dev_insight_tickets.ticket_id` references are nominal-only (SQLite doesn't enforce FK unless `foreign_keys=ON`). Gate: Michele's explicit go post-T-027b soak. Pre-deploy GitHub Action auto-snapshot is the rollback safety net. | Claude Code |
| T-027c | Notion-as-source migration Phase 5c — action_items decommission | Acknowledged data-loss decision from T-026 (`server.js:6031-6033`): `urgency`, `priority (1-100)`, `tools_to_use` columns have no Notion property. Decide port-to-Notion-page-body vs. accept loss. Then: remove SQLite `action_items` table via migration; strip `USE_NOTION_SOURCE` flag from `/api/actions` routes; delete SQLite-only legacy endpoints (`POST /api/actions/backfill-tickets`, `POST /api/actions/backfill-ticket-keys`, `PATCH/DELETE /api/actions/:id` SQLite branches); update dashboard SWOT query at `server.js:5894` to call Notion adapter or remove the action_items signal entirely. Gate: Michele's port-vs-loss decision + go post-T-027a shipped. | Claude Code |
| T-027d | Notion-as-source migration Phase 5d — dev_insight_tickets fate decision | T-020 (Dev Insights monthly review surface) deliberately keeps Dev Insights on SQLite. Therefore: `dev_insight_tickets` table likely stays put. Document the decision in `decisions-log.md`; close T-027d as "no-op, stays SQLite-resident per T-020 scope." If decision changes, expand into a migration. | Claude Code |
| T-027e | Notion-as-source migration Phase 5e — final cleanup | After T-027a + T-027c shipped: delete `services/notionSync.js`; remove the 5-min cron registration in `server.js` (~line 6760); remove `NOTION_SYNC_SINCE` env references; remove `USE_NOTION_SOURCE` flag from any remaining call sites (should be none after T-027a/c); remove both env vars from `.env.example`. Operational close-out: Michele removes `USE_NOTION_SOURCE` and `NOTION_SYNC_SINCE` from Railway dashboard (out-of-band of the repo diff). | Claude Code |
| T-013 | Rotate remaining 5 prod secrets (API_KEY, STRIPE_SECRET_KEY, QBO_CLIENT_ID, QBO_CLIENT_SECRET, ANTHROPIC_API_KEY) | Carryover from 2026-04-26 session. ADMIN_KEY rotated under T-012. Each of the 5 needs an external rotation in its own console (Stripe, Intuit Developer, Anthropic), then Railway env update. QBO pair also requires re-running OAuth flow. Worth deferring to a dedicated fresh session — not urgent, just hygiene. The previous "ticket 9ea1da15-..." reference does not point to a real ticket in any tracker (Notion 404, no match in dashboard tickets/action_items). | Claude Code |
| T-014 | Fix missing Sign Out button when logged in | Discovered 2026-04-28 during post-rotation smoke-test on prod. Logged in as `michele` on the dashboard root URL — sidebar shows all nav links (Overview through Finance) but the `.nav-user` div (`index.html:683`) with the username + Sign Out button never renders, leaving the user with no UI path to log out. Workaround documented today: `localStorage.removeItem('prism_token'); location.reload()` in DevTools. **Likely causes to investigate (in order):** (1) `hideLoginScreen()` at `index.html:4864` sets `#nav-user.style.display = 'flex'` — verify this fires after login and that the element is in the DOM at that point. (2) The `<nav>` may have `overflow:hidden` or a fixed height that clips the user block when the menu is long; check the nav CSS rules and the cascade. (3) `.nav-user` CSS may default-hide via media query or be missing layout rules. Repro: log in as any user at https://dashboard-api-production-dabe.up.railway.app/ (root, not /login.html), inspect the sidebar bottom. Expected: username + Sign Out button visible below "Invoices". | Claude Code |
| T-016 | Add pre-commit hook that runs `node scripts/sync-tasks.js` when TASKS.md is staged | Followup to T-015. Today the sync is manual and easy to forget — protocol updates in WORKFLOW.md §4 step 4 help, but a pre-commit hook makes it automatic and fail-loud. Hook should: detect TASKS.md in staged files; run sync; abort the commit if sync fails. Use husky or a plain `.git/hooks/pre-commit` script (latter is repo-portable). | Claude Code |
| T-017 | GitHub Action: run `node scripts/sync-tasks.js` on push to main (drift detector) | Followup to T-015. After PR merges, run sync from CI to catch drift between TASKS.md and the mirrors (e.g., if someone edits TASKS.md without running the script locally). Action should fail the workflow if it produces any creates/updates after the merge — that means the local sync was skipped, which is itself a workflow violation worth flagging. Needs `NOTION_API_KEY` and `DABE_API_KEY` GitHub secrets. | Claude Code |
| T-020 | Dev Insights review surface (monthly improvement loop) | Carved out of T-019 scope on 2026-04-30. Today the dashboard `tickets` table holds 46 `dev-insight` rows (auto-extracted from Claude Code session transcripts via `dev_insights_import.py`, run last session — count grows monthly). They're not editable tickets in the editorial sense — they're machine-generated friction signals (`buggy_code`, `wrong_approach`, etc.) that drive a continuous-improvement loop: review → adopt preventative measure (skill / hook / process change) or dismiss. T-019 hides them from the Tickets page; T-020 builds the dedicated UI: a Dev Insights page with monthly batch review, group-by friction type, inline disposition editing (`new` / `adopted` / `dismissed` / `duplicate`), free-text "preventative measure taken" field, "Convert to ticket" action that spawns a Notion follow-up ticket, and an archive view for prior months. Deliverable: one new page in `public/index.html`, schema additions for disposition fields, monthly cadence documented in WORKFLOW.md. | Claude Code |
| T-036d | Daily Agenda End of Day — orchestrator structured output contract (followup to T-036c) | Two-surface ticket. **Dashboard side:** add `top_10 TEXT` column to `daily_reviews` SQLite schema in `server.js` (~line 1000, before `summary`) with a safe migration via the `migrateCRMColumns` pattern; extend POST validators + INSERT/UPDATE statements at lines 4076–4115 to accept the new field. **Orchestrator side (outside this repo, in the Claude Scheduled skill folder at `~/OneDrive/Documents/Claude/Scheduled/prism-log-sync/SKILL.md` or whichever skill writes the daily summary):** stop concatenating everything into `summary`; populate the 9 existing category columns directly (clients_revenue / sprint_progress / development / marketing / training / services_pricing / infrastructure / other_milestones / reminders); write Top 10 as a JSON array of `{n, text, tag?}` to the new `top_10` column; reserve `summary` for a 2–4 sentence Narrative only. After T-036d ships, T-036c's markdown parser becomes dead-letter (used only for backfill / legacy entries). | Claude Code (dashboard side) + orchestrator-skill author (orchestrator side) |
| T-046 | Mission Control — Google Calendar OAuth + token storage (Phase 4 of T-036–T-055 roadmap) | Server-side OAuth flow mirroring the QuickBooks pattern at `server.js:5816+` (`/api/qbo/connect|callback|status`). New `services/googleCalendar.js` handles auth + token refresh + event fetch. New SQLite table `google_calendar_tokens` (single-row, same shape as `qbo_tokens`). New env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`. Scope: `https://www.googleapis.com/auth/calendar.readonly` (read-only sufficient for v1 Calendar page mirror). Account: Michele's primary `michele@prismaianalytics.com` only for v1 (multi-calendar = Phase 4.5 if needed). New routes: `/api/google-calendar/connect|callback|status|disconnect`. Graceful degradation: when not connected, Calendar page's Personal filter chip disabled with tooltip. **Pre-flight operational work (Michele, ~15 min):** create a Google Cloud project, enable Calendar API, register OAuth client, add the Railway redirect URI. **Leverage Brief:** `PRISM-Vault/Admin/Leverage-Briefs/T-046.md`. Independent of T-043/T-044 — Phase 4 work can begin in parallel with Phase 3 if pre-flight is done. | Michele (Google Cloud Console pre-flight) + Claude Code (code) |
| T-061 | Upgrade `node-quickbooks` to 2.0.43 to clear High-severity audit findings, then raise security-gate threshold from critical → high | Filed during T-060 when CI surfaced 4 High-severity prod-dep CVEs all rooted in node-quickbooks's pinned transitive deps (fast-xml-parser via GHSA-gh4j-gqv2-49f6, lodash via GHSA-r5fr-rjxr-66jc + GHSA-f23m-r3pf-42rh, underscore via GHSA-qpx9-hpmf-5gmw, uuid via GHSA-w5hq-g745-h8pq). `npm audit fix --force` would bump node-quickbooks 2.0.x → 2.0.43 — npm flags this as a breaking change against the live QBO integration at `server.js:5816+` (`/api/qbo/connect|callback|status`). Work: (1) upgrade node-quickbooks in a feature branch, (2) test the four QBO endpoints + the live OAuth-token-refresh path in services/quickbooksService.js, (3) confirm no breaking changes against current usage, (4) raise `--audit-level=critical` back to `--audit-level=high` in `.github/workflows/security-gate.yml`, (5) re-run audit and confirm clean. Estimated ~1h with QBO test setup. | Claude Code |
| T-059 | Fix `scripts/sync-tasks.js` to skip existing rows post-Notion-as-source cutover (or retire its SQLite-mirror write path) | Bug carried since T-026 cutover, originally flagged in T-031's close note. Today (2026-05-22) dry-run reports `54 created · 0 updated · 0 skipped` where it should report `0 created · 54 skipped`. Running for real would duplicate every row in the dashboard `tickets` table. Workflow currently skips the sync, which means TASKS.md edits never propagate to dashboard or Notion — the WORKFLOW.md §4 step 4 "Run the mirror sync" guidance is silently broken. Two paths: (a) teach the script that Notion-as-source means `tickets` rows are derived not authoritative — only update Notion, skip the dashboard write; (b) wait for T-027 (Phase 5 — drop SQLite `tickets` table) to make the script Notion-only by default. Option (b) is "do nothing, wait for T-027" — fine if T-027 ships soon, but leaves the bug live in the meantime. Filed during T-058 when sync-skip was hit. | Claude Code |

---

## Blocked

_(none yet)_

| ID | Title | Blocked by | Notes |
|----|-------|------------|-------|

---

## Done This Week

| ID | Title | Closed | Notes |
|----|-------|--------|-------|
| T-089 | Morning Brief stale-state banner — visible amber banner above the Morning Brief tile when dashboard falls back to yesterday's brief (`is_today === false`). | 2026-05-28 | Merged via PR #73 as `2a0bc2f` (squash) on 2026-05-28. Close-out bundled into T-090's first commit per WORKFLOW.md §4 — In Progress row carried stale from PR-merge timestamp until cleared on T-090 claim. Surgical 2-file frontend diff: `public/index.html:renderAgendaBrief` adds the `staleBanner` render path when `brief.is_today === false`; `public/styles/components.css` adds `.brief-stale-banner` using `--warning` token. No API change. Original trigger: 2026-05-28 07:41 orchestrator wake-task produced `0xC000013A` + 0-byte log, dashboard silently rendered prior day's brief. Companion orchestrator hardening tracked separately in workspace Notion ticket `36e236b6-b03a-813f-b945-c7fd531c92ee`. |
| T-088 | Reply & Resume composer workflow diagram — `public/knowledgebase/workflows/reply_and_resume_workflow.html` added to the Internal Workflows topic with `source:` front-matter pointing at `Development/dashboard/` for drift detection. | 2026-05-27 | Merged via PR #72 as `d407102` (squash) on 2026-05-27. Stale In Progress row carried from 2026-05-27 through 2026-05-28 morning until cleared by T-089's first commit per WORKFLOW.md §4. 4-view HTML (Overview / Procedural / Swim-Lane / Spec) using `prism_dashboard_workflow.html` as exemplar, generated via `/diagram-from-source` skill. Manifest regen via `node scripts/rebuild-knowledgebase-manifest.js` lands the new entry under admin-only visibility by default. Drift detection now fires on any dashboard change touching the composer surface (frontend `public/index.html` ticket modal composer + `ticketReplyAndResume()` JS handler, backend `server.js` Phase D1/D2 `[NEEDS-DECISION]` auto-flip in `POST /api/tickets/:id/comments`). T-087 close-out bundled into T-088's first commit (already documented in T-087's row below). |
| T-087 | Document the Plan file convention in `Development/dashboard/CLAUDE.md`. | 2026-05-26 | Merged via PR #71 as `8b73c74` (squash) on 2026-05-26. In Progress row was never cleared at merge time (recurring stale close-out pattern — see T-080/T-072 history); cleared as part of T-088's first commit per WORKFLOW.md §4. Notion ticket `36c236b6b03a81d18fbaecb54411b20d`. Short cross-reference section in dashboard CLAUDE.md points engineers at workspace-root CLAUDE.md for the canonical convention; surfaces the `Draft for review` marker requirement that `services/pendingPlansAggregator.js` `REVIEW_MARKERS` consumes; warns about near-misses like `Draft for approval` (silent-failure pattern). Documentation only, no code change. |
| T-086 | Refresh button on Daily Agenda Lifecycle header to bypass 60s server-side cache. | 2026-05-26 | Merged via PR #70 as `abdab6b` (squash). Railway auto-deploy verified (uptime 111s, `lifecycle-refresh-btn` + `{ refresh: true }` signatures present in live HTML). Canonical closing comment posted to Notion ticket `36c236b6-b03a-8167-a61d-ea3cd94929a4`; Status → Done. Diff: `public/index.html` +20/-3, `TASKS.md` +3/-1. Single `↻` button next to search input; `loadLifecyclePage()` extended with `{ refresh }` option appending `?refresh=1`; existing callers unchanged. Refreshes all five state sources at once via `lifecycleAggregator.compute(..., useCache: false)`. |
| T-083 | Handoff → Plan spawn action on Daily Agenda Lifecycle page. | 2026-05-26 | Merged via PR #69 as `a4d50bc` (squash). Railway auto-deploy verified (uptime 69s, `item.state === 'handoff'` + `spawnHandoffPlan` signatures live in prod HTML). Canonical closing comment posted to Notion ticket `36c236b6-b03a-81c9-a38f-d9555d4efd30`; Status → Done. Diff: `public/index.html` +39, `server.js` +16, `services/lifecycleAggregator.js` +157, `TASKS.md` +3/-1. Plus workspace-root `Handoffs/parseFrontMatter.js` (extended with `serializeFrontMatter` export — outside this repo, not git-tracked). CRITICAL slug allowlist + path-resolve containment + lstat symlink guard per peer-review. HIGH atomicity gate (frontmatter first, plan file second). 8 serializer unit tests + full E2E round-trip on real `2026-05-25_ChaosToCadenceRollout` (source bumped, plan created, auto-scan picked it up, all artifacts restored). |
| T-084 | Pending → Active promote + Dismiss actions on Daily Agenda Lifecycle page. | 2026-05-26 | Merged via PR #68 as `9175d39` (squash). Security Gate + Pre-deploy backup green; Railway auto-deploy verified (uptime 101s, `item.state === 'pending'` and `openPendingActivateRow` signatures present in live HTML). Canonical closing comment posted to Notion ticket `36c236b6-b03a-8193-be6a-dff43e141886`; Notion Status → Done. Diff: `public/index.html` +154, `server.js` +46, `services/lifecycleAggregator.js` +111, `services/pendingPlansAggregator.js` +34, `services/plansAggregator.js` +19, `TASKS.md` +3/-1. Self-healing dedup added (peer-review MEDIUM); CRITICAL slug allowlist defense in depth; HIGH endpoint naming aligned to `/pending-plans/:slug/activate`. Unblocks T-085 — `appendToManifest()` declared in this PR. |
| T-082 | Restore Inbox→Ticket promote action on Daily Agenda Lifecycle page (broken since T-078 dropped the Idea Vault accordion). | 2026-05-26 | Merged via PR #67 as `39abf83` (squash). Security Gate #62 (PR) + Security Gate #63 + Pre-deploy backup #62 (push-to-main) all green. Prod auto-deployed; container uptime confirmed 57s post-merge with `item.state === 'idea'` signature found in live HTML. Canonical closing comment posted to Notion ticket `36c236b6-b03a-8127-bb96-eaff34553fa9` and Notion Status flipped to Done. `public/index.html` +25 / -62 — Idea branch added to `lifecycleRowActions()` (gold pill + Dismiss + XSS-safe ↗ link); dead `loadInboxPanel`/`_renderInboxItem`/`_inboxEscapeHtml` cluster removed; refresh handlers in `triageInbox` + `submitInboxCapture` repointed to `loadLifecyclePage`. Reuses existing `triageInbox()` + `POST /api/mission-control/inbox/:id/triage` — no backend change. Three follow-up tickets filed (T-083 Handoff→Plan, T-084 Pending→Active+Dismiss, T-085 surface auto-scan suggestions); all peer-reviewed by 3 parallel agents before scope freeze with CRITICAL path-traversal + HIGH endpoint naming / atomicity / reuse findings folded in. |
| T-081 | Workflow diagrams in dashboard knowledge base — extend `scripts/rebuild-knowledgebase-manifest.js` with `Internal Workflows` TOPIC_RULE + optional `workflow-diagram:` front-matter parser + `hashSourceTree` helper + drift decoration in `scanCentralDropFolder`; land first diagram at `public/knowledgebase/workflows/prism_dashboard_workflow.html` under an admin-only KB topic (visibility defaults non-public via existing allowlist). | 2026-05-25 | Merged via PR #66 as `4e2b299` (squash). Both Security Gate #61 + Pre-deploy backup #61 green on push-to-main 7:44 PM EDT. Out-of-band of this PR (workspace-only edits, no dashboard touch): `prism-agents.plugin` was rebuilt this session (engineering-ops added, pod-strategy-ops removed) and CM-AI Framework BASELINE / CHANGE_LOG (entry #2 hash chain intact) / ROLLBACK / `cos-report-contract.json` updated to match — `verify-hash-chain.js` exit 0 on source + PRISM-Vault mirror. Companion `/diagram-from-source` slash command + workspace CLAUDE.md maintenance rule shipped separately. Stale In Progress row carried from 2026-05-25 7:44 PM EDT through 2026-05-26 ~12:00 EDT, cleared by T-082's first commit per WORKFLOW.md §4 (session ended without TASKS.md update — recurring pattern, see T-080/T-072 history). |
| T-080 | Dedupe duplicate `notionAdapter.listTickets({})` call in `lifecycleAggregator.compute()` | 2026-05-25 | Merged via PR #65 as `f2f532c9` (squash). Halves cold-load latency on `/api/mission-control/lifecycle` (3.3s → ~1.6s). Stale In Progress row cleared by T-081 claim per WORKFLOW.md §4 — close-out posted by the T-081 housekeeping commit because the prior Claude Code session ended before updating TASKS.md. |
| T-079 | Make the Mission Control Lifecycle Spectrum Detail card collapsible | 2026-05-25 | Merged via PR #64 (squash) — pending merge at the time of T-080's claim, SHA TBD. Close-out bundled into T-080's first commit per WORKFLOW.md §4 — both PRs ship in the same review window. Two-file change: `public/index.html` converts the Detail container to a `<details class="card agenda-collapsible" data-persist-key="agenda.lifecycle-detail">` (open by default; the row-count chip moves into the summary as an `.agenda-collapsible-count` so the count stays visible when folded); `TASKS.md` close-outs for T-077 + T-078. |
| T-078 | Mission Control Lifecycle Spectrum — 5-state view (Idea → Handoff → Pending → Active → Shipped) | 2026-05-25 | Merged via PR #63 as `e2990eb` (squash). Close-out bundled into T-079's first commit per WORKFLOW.md §4. New `services/lifecycleAggregator.js` (single aggregator with 60s cache + 5×5 workstream matrix), 2 new routes (`GET /api/mission-control/lifecycle`, `POST /api/mission-control/plans/:slug/ship` with 20-char closing-comment gate), 5 `--lifecycle-*` spectrum tokens, `PRISM_LIFECYCLE_COLORS` export, full frontend swap (KPI tiles + 3 Chart.js panels + sortable detail table with Ship CTA), `plansAggregator.js` extended with retired filter + `ready_to_ship` flag + raw read/write helpers, Solopreneur OS marked retired. 17 unit tests passing. Browser-verified live in production: tiles 1/0/4/2/4, velocity 0.9, cycle 2d, all 4 backfilled shipped plans visible. Resurrection of work originally drafted as T-076 (discarded mid-session by `git reset --hard`); T-076 was reassigned to Knowledgebase. Notion ticket PRISM-489 closed Done with canonical closing comment. **Followup:** lifecycle endpoint cold-call latency 3.3s (vs 0.2s warm) — duplicated `notionAdapter.listTickets({})` call inside `lifecycleAggregator.compute` (once at top, once via `plansAggregator.getActiveRoadmaps`). Candidate for a perf followup ticket. |
| T-077 | Portal Resources section — public-marked Knowledgebase items in client portal + archived-roadmaps backfill + active dedup | 2026-05-25 | Merged via PR #62 as `4f18073` (squash). Close-out bundled into T-078's first commit per WORKFLOW.md §4. New `GET /api/portal/knowledgebase` (public-only manifest, role-gate whitelisted), Resources card in `portal.html` with topic-grouped outline and content-type URL builder. Bundled side tasks: `config/archived-roadmaps.json` backfilled with 4 shipped multi-ticket arcs (tickets-page-v2, notion-as-source-migration, portal-domain-sweep, mission-control-morning-brief) with full schema (slug / name / ticket_ids / shipped_at / closing_comment / ticket_snapshot / cycle_time_days / workstream); `notion-as-source-migration` deduped out of `active-roadmaps.json`. Same library for every client in v1; per-client filtering deferred. Notion ticket closed Done with canonical closing comment. |
| T-076 | PRISM AI Knowledgebase page — internal-dashboard catalog of every Prism artifact (blogs, infographics, downloads, central drop folder) with hyperlink outline + per-row public/non-public curation | 2026-05-25 | Merged via PR #60 as `70dad5b` (squash). Close-out bundled into T-077's first commit per WORKFLOW.md §4. **What landed:** new `services/knowledgebaseScanner.js` + `services/knowledgebaseVisibility.js` + `scripts/rebuild-knowledgebase-manifest.js` + `config/knowledgebase-manifest.json` + `config/knowledgebase-visibility.json` + central drop folder at `public/knowledgebase/` (auto-served by express.static, README inside restates workflow) + new Knowledgebase page block in `public/index.html` (hyperlink outline, type/view/search filters, per-row ●/○ visibility toggle). 2 new routes: `GET /api/knowledgebase` (decorated with visibility), `POST /api/knowledgebase/visibility`. `npm run knowledgebase:rebuild` regenerates manifest locally; Railway only reads the committed JSON (mirrors T-057 plansAggregator pattern; `config/` already COPY'd into Docker image at Dockerfile line 59). Initial curation shipped: all 16 blog posts promoted to public; infographics + downloads stay non-public until reviewed. Post-merge prod smoke: `/health` 200 in 268ms, `/api/knowledgebase` 401 unauth (correct — requireAuth firing). T-074's TruffleHog fix held — Security Gate did not block this auto-deploy (2nd consecutive clean deploy after the 8 prior PRs that needed "Deploy commit" override). |
| T-075 | Working-tree hygiene — add `_*_tmp/` pattern to `.gitignore` + delete two stray untracked files | 2026-05-25 | Merged via PR #59 as `c30a82b` (squash). Close-out bundled into T-076's first commit per WORKFLOW.md §4 — In Progress row carried stale from earlier 2026-05-25 until cleared as part of T-076's claim. |
| T-074 | Fix TruffleHog `BASE and HEAD commits are the same` failure on push-to-main (root cause of "Deploy commit" workaround) | 2026-05-25 | Merged via PR #58 as `8598ec0` (squash). Close-out bundled into T-075's first commit per WORKFLOW.md §4. **Validation:** push-to-main Security Gate on `8598ec0` returned all 4 jobs green for the first time in this repo's documented history (TruffleHog had been failing on every prior merge — see PRs #50–57 + the T-067 revert). Prod `/health` 200 in 318ms. PRISM-485 stays open until 3-merge soak confirms Railway auto-deploys without the manual "Deploy commit" override. After the soak: close PRISM-485 Done and update WORKFLOW.md §4 / DEPLOY_RUNBOOK.md to remove the (now-fixed) workaround from operator memory. |
| T-073 | Daily Agenda — side-by-side `Recommended for To Do` + `Past-due in Backlog` panels with `created` + `due` date chips | 2026-05-25 | Merged via PR #57 as `c08def8` (squash). Close-out bundled into T-074's first commit per WORKFLOW.md §4. Backend `created_at` exposed in both endpoints, frontend 2-column grid + date chips shipped. |
| T-072 | Read-only modal cleanup — drop redundant "This ticket is mirrored from Notion..." sentence | 2026-05-25 | Merged via PR #56 as `2514201` (squash). Pre-squash commits: `353d2c6` (feat) + `f7d15f9` (T-071 close-out + T-072 claim). Standalone close-out — no follow-on task was claimed at merge time, so the In Progress row carried stale across 2026-05-24 evening → 2026-05-25 morning until cleared in this commit. **What landed:** removed the explanatory sentence ("This ticket is mirrored from Notion...") from `ticketShowDetailReadOnly` in `public/index.html` (around line 3387). The 🔒 view-only chip in the top strip and the "Open in Notion ↗" button already convey what the sentence said. Verified on `main`: grep for "mirrored from Notion" against `public/index.html` returns zero matches. **sync-tasks.js not run** per T-059 (Notion-mirror write path is broken — TASKS.md edits do not propagate to the dashboard `tickets` table or Notion DB; close-out lives in this file only). No matching dashboard/Notion ticket exists for T-072, so no canonical closing comment to post via `/api/tickets/:id/comments`. |
| T-071 | Tickets page v2 — Phases A–F + Michele's polish + side fixes | 2026-05-24 | Merged via PR #55 as `530dd64` (squash). Close-out bundled into T-072's first commit per WORKFLOW.md §4. Contained 5 commits at merge: T-071 main feat, two openTicketByKey fix tiers (title-prefix + NTN-uuid → notion_page_id fallback), housekeeping. ~1,922 lines added. End-to-end smoke verified in local preview throughout session. Railway auto-deployed to prod on merge. |
| T-027b | Notion-as-source migration Phase 5b — Notion comments adapter | 2026-05-24 | Merged via PR #53 as `2ee6bd1` (squash). Close-out bundled into T-071's first commit per WORKFLOW.md §4 — the In Progress row was left stale on `main` after #53 merged earlier today; cleared as part of T-071's housekeeping commit. |
| Phase 2D | Status-vocab close-out — extend sync-tasks.js and notionSync.js to the 7-option Notion status vocabulary | 2026-05-24 | Merged via PR #54 as `3cb032f` (squash). Same close-out gap as T-027b — In Progress row was not updated; cleared as part of T-071's housekeeping commit. |
| T-027 | Notion-as-source migration Phase 5 — original spec | 2026-05-24 | **No merge SHA — subsumed by T-027a/b/c/d/e after scope discovery.** Original spec ("drop tickets + action_items tables, delete cron, remove flag") under-specified the actual touch-points: (1) `ticket_comments` FK + `/api/tickets/:id/comments` endpoints are load-bearing for the workspace-root CLAUDE.md "needs decision → PATCH Review + POST comment" protocol — would silently break without a Notion comments adapter; (2) `action_items` columns (`urgency`, `priority 1-100`, `tools_to_use`) have no Notion property and require a port-vs-loss decision flagged at `server.js:6031-6033`; (3) several legacy SQLite-only endpoints (`/api/actions/backfill-*`, dashboard SWOT query at `server.js:5894`) still hit the tables; (4) `dev_insight_tickets` table is intentionally SQLite-resident per T-020 scope. Decomposed 2026-05-24 into 5 sub-tickets: **T-027b** (comments adapter — ships first, prerequisite), **T-027a** (drop `tickets` table), **T-027c** (action_items decommission), **T-027d** (no-op decision on `dev_insight_tickets`), **T-027e** (final cleanup). See decisions-log 2026-05-24 for full reality-check. |
| T-070 | Add Founder Journey personal-account pilot to Active Plans manifest (config/active-roadmaps.json) | 2026-05-24 | Merged via PR #52 as `9bd40a3` (squash). Close-out bundled into T-027b's first commit per WORKFLOW.md §4. Founder-journey work tracked under Notion SOC-005 + sub-tickets F-001/F-002/F-003 per `PRISM-Vault/Marketing/founder-journey/master-plan.md`. |
| T-069 | Fix dashboard unusable on mobile (iPhone Chrome) — main shoved off-screen + nav drawer not sliding | 2026-05-23 | Merged via PR #51 as `dbd28a3` (squash). Close-out bundled into T-070's first commit per WORKFLOW.md §4. Root cause: components.css line 498 set `body.prism-themed nav { position: relative; width: 100% }` inside the mobile @media, which broke fixed-positioning and made nav a flex child of `.shell`, pushing `<main>` off-screen. Fix: restored slide-drawer pattern with `position: fixed` + `transform: translateX(-100%)` defaulted, `translateX(0)` when `body.nav-open`. Verified at 375×812 (main left:0, full-width, drawer toggles correctly) and 1280×800 (no regression). Live verified on iPhone Chrome in incognito (cache bypass) post-deploy. Note: Railway auto-deploy initially blocked by Security Gate CI failure (T-061 node-quickbooks CVEs); manual "Deploy commit" override needed per the T-062/T-063 recurring pattern. |
| T-067 (v2) | Portal domain sweep (v2) — replace hardcoded `dashboard-api-production-dabe.up.railway.app` with `portal.prismaianalytics.com` across scripts, CI workflow, `_redirects`, build-site.sh, and active operator docs | 2026-05-23 | Merged via PR #50 as `f027406` (squash). Close-out bundled into T-069's first commit per WORKFLOW.md §4. DNS + SSL for `portal.prismaianalytics.com` verified live before re-merging — Railway custom domain on dashboard-api service confirmed `Server: railway-edge` via `--resolve` bypass at 21:37 UTC, then full resolution post-Netlify-DNS-CNAME. |
| T-068 | SECURITY — client role can see admin dashboard via magic-link login (Bug E). Fix /portal route, add role-gate middleware on /api/*, redirect client role away from index.html in frontend boot | 2026-05-23 | Merged via PR #47 as `bd12441` (squash). Close-out bundled into T-067 (v2)'s first commit per WORKFLOW.md §4 — note this housekeeping initially landed in the v1 T-067 PR #48 (`2eb0c72`) but was reverted along with the rest of that PR; re-applying here. Three-layer defense added: (1) `extensions: ['html']` on express.static so /portal serves portal.html, (2) role-gate middleware after `app.use('/api', requireAuth)` whitelisting /auth/me, /auth/logout, /portal/me, /services, POST /assessments — everything else 403 for client role, (3) frontend boot/handleLogin redirect to /portal when user.role==='client'. Bonus: `requireAuth` now accepts prism_session HttpOnly cookie fallback. New `GET /api/portal/me` endpoint returns `{contact, client, assessment}` for the authenticated client. End-to-end verified 21:06 UTC via `michele.fisher33+welcome-v3@gmail.com`. Notion ticket: https://www.notion.so/369236b6b03a810d80ebd2dcd1395a0b (Done). |
| T-067 (v1, reverted) | First attempt at portal-domain sweep | 2026-05-23 | Merged via PR #48 as `2eb0c72`, reverted via PR #49 as `bdd981d`. Lesson: the operational setup (Railway custom domain + Netlify DNS CNAME for `portal.prismaianalytics.com`) was outlined but never executed before the code sweep merged. Result: CI pre-deploy backup workflow #44 failed (curled non-resolving URL), and the website login button at https://prismaianalytics.com/login/ went dead. Both PRs reverted within ~10 min. Lesson recorded for future infra-dependent sweeps: ALWAYS `nslookup` the target domain via a public resolver (8.8.8.8 / 1.1.1.1) before merging. T-067 retried as v2 after Michele added the Railway custom domain (`portal.prismaianalytics.com` → port 8080, SSL provisioned, `Server: railway-edge` confirmed via `--resolve` bypass at 21:37 UTC). |
| T-066 | Lead intake completion — install `resend` (Bug D) + insert into `leads` table inside POST /api/leads tx (Bug B) | 2026-05-23 | Merged via PR #46 as `7c618d6` (squash). Close-out bundled into T-068's first commit per WORKFLOW.md §4. Closed the website-form investigation thread (Bugs A/B/C/D). End-to-end verified live submission → welcome email → magic link → production URL. Discovery during verification: clicking the magic link landed the lead in the **admin dashboard** instead of the client portal — Bug E (privilege escalation), filed as T-068. T-067 (portal-domain hardcoded-URL sweep) was started after T-066 but paused mid-edits when Bug E was discovered. Notion ticket: https://www.notion.so/369236b6b03a818b89b6efe47d45d15b. |
| T-065 | Pending Plans panel on Daily Agenda (3rd column beside Idea Vault + Active Plans) | 2026-05-23 | Merged via PR #45 as `48998b5` (squash). Close-out bundled into T-066's first commit per WORKFLOW.md §4. |
| T-064 | Idea Vault rendering polish — drop "by " prefix on capture attribution | 2026-05-23 | Merged via PR #44 as `06ca6ee` (squash). Close-out bundled into T-065's first commit per WORKFLOW.md §4. One-line tweak in `_renderInboxItem` (public/index.html ~line 4895) — capture attribution now renders as `michele · 1:02 PM` instead of `by michele · 1:02 PM`. Per Michele 2026-05-23: username and timestamp on the same line are self-explanatory; the `by ` prefix added visual weight without adding information. |
| T-063 | Hotfix Plans manifest never reaches Railway (rename data/ → config/, COPY into Docker image) | 2026-05-23 | Merged via PR #43 as `63862a5` (squash). Close-out bundled into T-064's first commit per WORKFLOW.md §4. T-057 shipped `data/active-roadmaps.json` but Dockerfile didn't `COPY data/`, AND `/app/data` is the Railway-mounted SQLite volume which would have overlaid any baked-in files anyway. Renamed to `config/active-roadmaps.json` (no volume collision), added `COPY config/ ./config/` to the Dockerfile right after the analogous T-036h `COPY reports/` block, updated `plansAggregator.js` MANIFEST_PATH. Post-merge prod smoke green: `notion_available: true`, 3 roadmaps populated, same data as local. Required the now-standard Railway "Deploy commit" manual override on the SKIPPED row because Security Gate CI still red (T-061 outstanding). |
| T-057 | Mission Control — Plans panel on Daily Agenda (Notion-sourced active roadmaps) | 2026-05-23 | Merged via PR #42 as `6a1ec39` (squash). Close-out bundled into T-063's first commit per WORKFLOW.md §4. Shipped: new `services/plansAggregator.js` (manifest reader + title-based ticket ID extractor + dedup-by-ID using status-precedence to handle Notion's 5x-duplicate pattern); `GET /api/mission-control/plans` route (requireAuth, graceful Notion-unavailable fallback); seed manifest with 3 roadmaps (Mission Control T-036..T-055, Notion-source migration T-023..T-027, Solopreneur OS PS-057/058/059); frontend `#agenda-plans-card` rendered as Active Plans card (progress bar + in-progress headline + blocked count). Followups in same PR (commit `bd62d9e`): (a) Inbox + Active Plans wrapped in 2-column CSS grid above the Morning Brief, (b) user-visible "Inbox" → "Idea Vault" rename (6 strings), (c) Notion `Captured By` attribution on captures with graceful fallback when the property doesn't exist in the Notion schema yet. **Bug shipped:** manifest lived in `data/active-roadmaps.json` but Dockerfile never COPY'd `data/`, AND /app/data is the Railway SQLite volume mount which overlays anything COPY'd there. Result on prod: `/api/mission-control/plans` returns empty manifest. T-063 is the hotfix. |
| T-062 | Daily artifacts table + brief POST endpoint + kill vault-sourced surfaces | 2026-05-23 | Merged via PR #41 as `f6a7b13` (squash). Close-out bundled into T-057's first commit per WORKFLOW.md §4. Shipped: new `daily_artifacts(date_iso, kind, content, content_type, metadata_json, created_at, updated_at)` SQLite table with composite PK and `(kind, date_iso DESC)` index; `POST /api/daily-artifacts/:kind/:date` endpoint with `requireAuth` + kind allowlist (`morning_brief` only) + 512KB cap + `ON CONFLICT DO UPDATE`; two DB-prefer wrappers (`mcResolveBriefForDate`, `mcResolveTodayOrYesterdayBrief`) with `source: 'db'\|'file'` canary on the GET response. Same-PR cleanup removed the Recent Decisions card (read PRISM-Vault on local FS, always empty on Railway), slimmed `mcReadTodayContext` to `{date, brief_headline}`, and deleted `mcReadRecentDecisions`/`mcReadVaultFile`/`mcResolveVaultRoot`/`MC_VAULT_DEFAULT`/`MC_DECISIONS_LIMIT` (zero callers after edits). Orchestrator side (out-of-band per T-022/T-039 precedent): `chloe-morning-brief/SKILL.md` Step 7 added non-fatal PowerShell POST after the local file write. **Railway deploy required manual override** — Security Gate CI gate caused auto-deploy SKIP, then dashboard "Redeploy" pulled cached old image; resolved via Railway "Deploy commit" menu on the SKIPPED row. Filed as followup (T-063 candidate): CI Security Gate failures silently blocking Railway deploys (T-061 still the root fix). Post-merge prod smoke all green: source=file fallback works, POST sentinel 201, source=db end-to-end with today's actual brief, `/api/mission-control/today` slim shape preserved. Tomorrow ~7:30 AM orchestrator run is the real test. |
| T-060 | Add Security Gate CI workflow (npm audit / Semgrep / TruffleHog / CycloneDX SBOM) | 2026-05-22 | Merged via PR #39 as `ee82a22` (squash). Close-out posted by this housekeeping commit. Implements Layer 1B controls 7.2 (SAST + SCA on release) and SBOM emission per the CIS-style hardening baseline at `CIS Benchmarks/dev-surface-hardening-baseline.html`. Five-iteration CI tuning cycle: (1) gitleaks-action@v2 requires paid org license → switched to TruffleHog `--only-verified` (free, more capable); (2) Semgrep `p/nodejs-best-practice` and (3) `p/express` both 404 on registry → kept `p/owasp-top-ten` + `p/javascript`; (4) npm audit surfaced 4 High-severity transitives all in `node-quickbooks` (filed T-061 to upgrade and restore `--audit-level=high`; currently set to `critical`); (5) Semgrep flagged Dockerfile missing-USER → architectural decision per Dockerfile:56 (Railway volume needs root for SQLite WAL), suppressed with `--exclude-rule` and full rationale block in the workflow. SBOM job runs on push-to-main only (release artifact), uploads CycloneDX JSON with 90-day retention. |
| T-058 | Daily Agenda — remove KPI tiles row + Today card | 2026-05-22 | Merged via PR #38 as `0de624c` (squash). Close-out posted by T-060's first commit per WORKFLOW.md §4 (lock was held by T-058 for ~6 hours past merge before cleared). Removed KPI tiles row + Today card from Daily Agenda per the simplification thread following T-036e/g/h's morning-brief surface taking over the above-the-fold attention. |
| T-038 | Mission Control — Daily ticket snapshot cron (Phase 1 of T-036–T-055 roadmap) | 2026-05-22 | Merged via PR #37 as `56b7143` (squash). Close-out bundled into T-058's first commit per WORKFLOW.md §4. Nightly cron writes `reports/ticket-snapshot-YYYY-MM-DD.json` from `/api/tickets/summary` (excluding `category=dev_insight`); idempotent same-day overwrite. Powers the 14-day burndown viz in T-050. Railway scheduler config remains out-of-band of the repo diff. |
| T-026a | Notion adapter — treat empty-mappable updates as no-ops instead of 400 (unblocks sync-tasks.js linkBack + business-health-eval progress_update) | 2026-05-22 | Merged via PR #36 as `08c6ee6` (squash). Close-out bundled into T-038's first commit per WORKFLOW.md §4. Adapter's `updateTicket` and `updateActionItem` previously returned 400 when called with a payload that contained only un-mapped properties (e.g., `progress_update` from business-health-eval, which the dashboard SQLite schema accepts but the Notion DB has no equivalent property for). Fix: when the resulting Notion properties object is empty, return a no-op success instead of throwing — preserves caller contract and unblocks the linkBack flow in `sync-tasks.js`. Includes contract test coverage. |
| T-036h | Dockerfile — include `reports/` in production image (fixes broken brief surface on Railway) | 2026-05-22 | Merged via PR #35 as `94c3ff6` (squash). Close-out bundled into T-026a's first commit per WORKFLOW.md §4. One-line Dockerfile fix (`COPY reports/ ./reports/`) so the deployed container has the 22 morning-brief markdown files T-036e's renderer reads. Closes the root cause flagged in T-036g's close note — `attached_brief: null` on every End-of-Day row and `/api/mission-control/morning-brief` returning `available:false` were both downstream of the same missing COPY. Lesson recorded: "files in the repo ≠ files on Railway when the build is Dockerfile-driven with explicit COPY lists." |
| T-036g | Per-day collapsible End of Day cards + attach morning brief to each day's Top 10 | 2026-05-22 | Merged via PR #34 as `e79f4f1` (squash). Close-out bundled into T-036h's first commit per WORKFLOW.md §4. Surface shipped but **non-functional on prod** — discovered post-merge during the 2026-05-22 orchestrator run: the `attached_brief` field comes back null on every row because Railway's deployed container has no `reports/` directory at all (Dockerfile never `COPY`'d it). Same root cause kills `/api/mission-control/morning-brief`. T-036h is the one-line Dockerfile fix. Validates the lesson that "files in the repo" ≠ "files on Railway" when the build is Dockerfile-driven with explicit COPY lists. |
| T-036f | Deploy morning brief files to Railway (gitignore carve-out + stage 22 historical briefs) | 2026-05-22 | Merged via PR #33 as `e656c39` (squash). Close-out bundled into T-036g's first commit per WORKFLOW.md §4. Original PR also touched .gitignore to carve out morning briefs from the blanket `reports/` rule, but Michele preserved the blanket rule — the 22 brief markdown files are now tracked individually (gitignore doesn't un-track already-tracked files), so future briefs will need `git add -f` or explicit listing. Net effect: production now has the 22 historical briefs deployed and the T-036e renderer reads them on the live dashboard. |
| T-036e | Surface morning brief on Daily Agenda — Top 10 above the fold + Needs Michele / Carry-forward / Bucket counts sections | 2026-05-22 | Merged via PR #32 as `27d952a` (squash). Close-out bundled into T-036f's first commit per WORKFLOW.md §4. Backend `mcReadMorningBriefFile` / `mcSplitFrontmatter` / `mcParseBriefSections` / `mcParseTopTen` + `GET /api/mission-control/morning-brief` endpoint. Frontend `#agenda-brief` section above the fold with brand-gold subheader, gold-railed narrative block, bucket chips (non-zero in gold), Top 10 with mono-gold numerals + bold leads via `inlineMd`, collapsible Needs Michele (open) / Carry-forward / Maturity / What landed / Data hygiene (closed). CSS `.brief-*` block. One-line carry-over fix to T-036c's `renderTopTen` so bold leads in End-of-Day heuristic Top 10 also render. **Discovered post-merge:** the morning brief markdown files were gitignored, so they shipped to production with no data — endpoint returned `{ available: false }`. T-036f (this commit) carves them out of the gitignore and stages 22 historical briefs so the deployed dashboard has data to render. |
| T-036c | Daily Agenda End of Day visual hierarchy fix (hotfix — markdown rendering + Top 10 + categorization) | 2026-05-22 | Merged via PR #31 as `8cddf57` (squash). Close-out bundled into T-036e's first commit per WORKFLOW.md §4. Three-zone editorial layout (header strip + Top 10 + category grid + muted Narrative footer) shipped. Mini-markdown parser (`parseMiniMarkdown` + `inlineMd`) with HTML-escape-first defense. `extractTopTen` heuristic lifts numbered items from `summary`. Security review caught a stored-XSS vector on unescaped `review_date` in `onclick` attribute → fixed via server-side regex validation in POST + client-side `encodeURIComponent`+`escapeHtml`. Duplicate `escHtml`/`escapeHtml` deduplicated. Post-merge review: T-036c's Top 10 source is wrong layer — canonical Top 10 lives in the orchestrator's morning brief file at `reports/cos-morning-brief-YYYY-MM-DD.md`, not in `daily_reviews.summary`. T-036e (this commit) surfaces the brief on Daily Agenda above the fold and supersedes T-036c's End-of-Day Top 10 as the canonical surface (T-036c's heuristic stays for legacy entries). |
| T-044 | Mission Control — Excel → Notion Content Calendar migration script + cutover (Phase 3 of T-036–T-055 roadmap) | 2026-05-22 | Merged via PR #30 as `06254cb` (squash). Close-out bundled into T-036c's first commit per WORKFLOW.md §4. Migration script + mapping JSON + dry-run report shipped. 70 rows planned across May + June sheets, 0 validation failures, 15 informational warnings (all `Other`-bucket Format values). Live cutover scheduled for Saturday 2026-05-24 per the open-questions resolution; Michele runs `node scripts/migrate-content-calendar.js` without `--dry-run`, spot-checks 5–10 Notion pages, then archives the Excel file to `Marketing/Archive/`. Both pre-build resolutions from yesterday (Platform multi-select, Linked Asset split) applied in the schema. |
| T-043 | Mission Control — Notion Content Calendar DB schema + creation (Phase 3 of T-036–T-055 roadmap) | 2026-05-22 | Merged via PR #29 as `e871ca4` (squash). Close-out bundled into T-044's first commit per WORKFLOW.md §4. Notion DB created at https://www.notion.so/249b5785476a4e609a5836827cd52111 with 10 properties (both pre-build resolutions applied: Platform → multi-select; Linked Asset → Staging Path + Published URL). Schema doc shipped at `docs/content-calendar-schema.md`; `NOTION_CONTENT_CALENDAR_DB_ID` added to `.env.example` with the real DB ID. Operational follow-up: set the same value in Railway's Variables tab. |
| T-036b | Cap End of Day section to last 10 days (Daily Agenda perf fix) | 2026-05-21 | Merged via PR #28 as `201c5ad` (squash). Close-out bundled into T-043's first commit per WORKFLOW.md §4. Surgical perf fix on the Daily Agenda page — the End of Day collapsible was rendering every historical daily review (unbounded growth). Capped to last 10 days, keeping the page weight bounded as the journal accumulates. |
| T-039 | Mission Control — Chloe writer behavior + workspace-root CLAUDE.md update (Phase 1 of T-036–T-055 roadmap) | 2026-05-21 | **No merge SHA — non-code, out-of-band per T-022 precedent.** Workspace-root files edited in place; this TASKS.md row is the dashboard-repo bookkeeping. Shipped: (a) workspace-root [CLAUDE.md](../../../CLAUDE.md) "Strategic Chief of Staff — Chloe" section expanded with four-mode framing (Reader / Writer / Triager / Synthesizer), auto-load now pulls Inbox count + Needs-decision count from the dashboard, channel-agnostic phrasing ("every Claude Code or Cowork session" — not Cowork-only); (b) new project-scoped memory entry `feedback_chloe_writer_behavior.md` with index pointer in `MEMORY.md`; (c) `PRISM-Vault/Admin/CoS/README.md` expanded with the four-mode table + dashboard-endpoints reference + updated invoke section. Three pre-build decisions confirmed with Michele at pickup: surface threshold = count > 0 (always mention non-zero), synthesis cadence = first Claude Code or Cowork session of each week (channel-agnostic — Michele's primary surface is Claude Code, not Cowork), scope = all sessions (not Cowork-only). Unblocks T-039's downstream — Chloe will write Inbox captures starting this session forward. |
| T-037 | Mission Control — Inbox capture + panel (Phase 1 of T-036–T-055 roadmap) | 2026-05-21 | Merged via PR #26 as `0e5b566` (squash). Close-out bundled into T-039's first commit per WORKFLOW.md §4. Phase 1 of the Mission Control roadmap. Shipped: sticky Inbox capture bar at the top of `<main>` visible on every page (optimistic UI, Enter or click to submit); Inbox panel on Daily Agenda between Recent Decisions and End of Day (newest first, SLA banner at >20); `services/inboxRouter.js` (testable create/list/triage logic, 60-char title derivation, `cowork:inbox` source convention); `services/notionAdapter.js` extended with `source_prefix` filter + `source_raw` field; 3 routes under `/api/mission-control/inbox*` (create, list, triage). Pre-build decisions: SLA cap=20, source=`cowork:inbox`, title=60 chars, panel default=visible. Out-of-scope deferrals: T-039 Chloe writer (shipped same session), T-040 vault writer for `→ Open question` action (currently 503 fallback), v2 category+due_date UI on triage-to-Ticket. Local boot + accessibility-tree snapshot + zero console errors confirmed pre-merge; live E2E (capture → list → triage) deferred to post-merge Notion-creds path. |
| T-036a | Hotfix — route post-login bootstrap to `loaders['daily-agenda']` so the new landing page actually populates | 2026-05-21 | Merged via PR #25 as `8491e03` (squash). Close-out bundled into T-037's first commit per WORKFLOW.md §4. Surgical 3-line fix swapping `loaders.dashboard()` → `loaders['daily-agenda']()` at three post-auth call sites (handleLogin success path ~5239, boot() session-token validation ~5276, boot() URL-key validation ~5286). All five `loaders[...]()` matches in index.html now point at the correct landing page; the renamed Dashboard (KPIs) page is still reachable via nav and its loader still fires through `showPage()`. Verified locally pre-commit: post-login tile row populates with 4 KPIs, Today card shows morning-brief headline, Recent Decisions card renders 5 entries with Today/Yesterday labels. Prod health check post-merge confirmed deployed. |
| T-036 | Repurpose Daily Review → Daily Agenda + Today panel + nav reorganization + default landing swap | 2026-05-21 | Merged via PR #24 as `cf904d6` (squash). Close-out bundled into T-036a's first commit per WORKFLOW.md §4. Phase 1 of the Mission Control roadmap (T-036–T-055). Shipped: nav reorganized into 4 groups (Daily / Clients & Sales / Finance / More with `<details>` collapsible), Dashboard renamed to KPIs, default landing swapped to Daily Agenda, page rebuilt with Today tiles + Today context + Recent Decisions cards + End of Day collapsible wrapping the existing journal form, new aggregator `GET /api/mission-control/today` with vault path-traversal containment via `path.resolve` + `fs.realpathSync`. Post-merge regression caught by Michele via screenshot: post-login bootstrap still calls `loaders.dashboard()` so the Daily Agenda loader never fires — page shows "Loading…" placeholders. T-036a is the surgical 3-line fix. |
| T-035 | Per-page :root cleanup — delete duplicated index.html :root, add --bg back-compat alias | 2026-05-21 | Merged via PR #23 as `2b89fe1` (squash). Close-out bundled into T-036's first commit per WORKFLOW.md §4. Scope disciplined to index.html only — login/training/prism-ai-readiness-assessment retained their page-specific `:root` blocks (deferred to a later deep-cleanup ticket; T-035's commit message referenced "T-037" as that followup, but T-037 was taken by the Mission Control Inbox ticket per sequential-ID convention — the deeper :root cleanup will get whatever next number is available when it's filed). Intentional status-color shift to brand palette (--green/--red/--amber/--purple/--text/--muted now flow from tokens.css's body.prism-themed) — last island of Tailwind defaults inside index.html. Verified live preview against navy --frame bodyBg and all 14 var(--X) references in index.html's embedded styles. Risk: low, reversible. |
| T-029 | Build Pulse mission-control page in dashboard (`/pulse` route) | 2026-05-21 | **No merge SHA — subsumed by the Mission Control roadmap (T-036–T-055).** Original brief filed 2026-05-11; deferred until after May 17 launch. Launch shipped on schedule. Reopened 2026-05-21 in a Cowork session that diagnosed the broader "five surfaces all claiming to be where I check what's happening today" problem and expanded T-029's single-page scope into a 20-ticket roadmap covering: (a) Daily Agenda repurpose of `#page-daily-review` rather than adding a parallel `/pulse` page; (b) dedicated Calendar page replacing Excel entirely; (c) Chloe role expansion to writer + cross-session synthesizer + Inbox triager; (d) nav reorganization into four groups; (e) hard demotion of Excel + Cowork Live artifact gated on a 14-day trust window. Plan file: `~/.claude/plans/please-review-issues-confusion-with-spicy-flamingo.md`. T-029.md leverage brief preserved with a subsumption notice; new briefs filed at `PRISM-Vault/Admin/Leverage-Briefs/T-036.md` through `T-055.md`. T-036 starts Phase 1 immediately. |
| T-034 | Fix Magnolia AI Readiness seed-date anomaly (T-192 follow-up cleanup) | 2026-05-20 | Merged via PR #22 as `a7dc588` (squash). Close-out bundled into T-035's first commit per WORKFLOW.md §4. |
| T-033 | Dark-mode polish — de-purple surface scale, strip Unicode nav icons, tighten nav vertical density | 2026-05-19 | Merged via PR #21 as `a77c158` (squash). Close-out bundled into T-034's first commit per WORKFLOW.md §4. Visual polish on top of T-032's Prism Dark activation: surface scale de-purpled (cleaner neutrals at `--shell`/`--shell-elevated`/`--core`), Unicode nav glyphs stripped to keep typography clinical, nav vertical density tightened. Files changed: `public/index.html`, `public/styles/components.css`, `public/styles/tokens.css`, `TASKS.md`. No production-wipe risk — pure CSS/HTML polish. |
| T-032 | Brand-aligned dark retheme — activate Prism Dark theme, swap Chart.js palettes, migrate logo to SVG | 2026-05-18 | Merged via PR #20 as `277f7c3` (squash). Close-out bundled into T-033's first commit per WORKFLOW.md §4. Activated `body.prism-themed` on 21 of 22 pages (portal.html held LIGHT by intent — client-facing surface; needs separate brand decision + prism-themed-light variant before unifying). Index.html: `window.PRISM` block injected after Chart.js CDN exposes PALETTE / STATUS / DRIFT / AUTOMATION / CHART_AXIS as single source of truth; all three Tailwind palettes (catColors×2, finPalette) + chartColors + driftColorMap + status-array swapped to `window.PRISM.*`; borderColor `#1A1A2E` → `#0F0C30` ×2; logo PNG → SVG. Prod sentinels green: tailwind chart colors gone, portal stays light, themed pages activate cleanly. Visual-review caveat: headless screenshots timed out locally — Michele verified in real browser post-deploy and flagged three issues handled in T-033 (purple cast, nav icons, nav density). |
| T-031 | Brand CSS foundation — extract shared tokens/base/components, link from all dashboard pages (no visual change) | 2026-05-18 | Merged via PR #19 as `bacd5fe` (squash). Close-out bundled into T-032's first commit per WORKFLOW.md §4. Shipped 4 stylesheets under `public/styles/`, the SVG logo, an inject script, plus `<link>` tags on all 22 HTML pages — all scoped to `body.prism-themed` so the foundation was inert in prod (verified post-deploy: 5/5 new assets serve 200, sentinel `--frame` and `--grad-chrome` present, no embedded :root displaced). Two refinement commits folded in the updated Prismatic Clarity philosophy: surface vocabulary renamed to frame/shell/shell-elevated/core, seams bumped (border 0.18 / strong 0.30 + new --seam token), radii disciplined to 3 (sm/lg/pill), --grad-gold renamed to --grad-chrome. Followup operational gap: `scripts/sync-tasks.js` is misbehaving post-T-026 (dry-run reports 33 created when it should skip everything) — file new ticket to either teach the script about Notion-as-source or retire its SQLite-mirror write path after T-027. |
| T-026 | Notion-as-source migration Phase 4 — write-path cutover, flip flag, short-circuit cron | 2026-05-18 | Merged via PR #18 as `d397e1e` (squash). Close-out bundled into T-031's first commit per WORKFLOW.md §4. Phase 4 of 5 complete; T-027 (Phase 5 — snapshot-then-remove SQLite tables) is the next gate, blocked on Michele's explicit go-ahead post-soak. |
| T-021 | Notion "Prism AI Tickets" DB cleanup (pre-sync legacy triage) | 2026-05-16 | Merged via PR #__ (TBD). Adds `scripts/apply-notion-cleanup.js` — parses a K/A/D-marked triage doc from `scripts/notion-tickets-inventory.js` and archives via Notion API; 2 req/sec throttle (under Notion's 3/s ceiling); flags `--dry-run`/`--limit=N`/`--yes`; rollback log in `reports/cleanup-log-YYYY-MM-DD.jsonl` for the 30-day Notion-trash window. **Executed against Notion prod 2026-05-16 18:37 UTC** with the user's authorization: triage doc found 11 duplicate clusters across 66 pages (Business Health Auto-Eval ran ~7× without idempotency); 55 archived (1 keep per cluster — selected by status priority: In progress > lowest-PRISM-ID Not started > lowest-PRISM-ID Done when majority were Done). Post-cleanup `notion-tickets-inventory.js` re-run confirms **0 duplicate clusters**; total pages dropped 234 → 179. Operational followup: `NOTION_SYNC_SINCE=2026-04-30` env var on Railway can now be removed (not in this PR — separate operational change). Followup risk: T-027/Phase 5 still needs the idempotency guard on the auto-eval script, otherwise dupes regenerate on the next run. |
| T-025 | Notion-as-source migration Phase 3 — reconciliation backfill from annotated CSV | 2026-05-16 | Merged via PR #__ (TBD). Adds `scripts/apply-reconciliation.js` (Notion-only writes; SQLite untouched since tables drop in T-026/T-027) + `scripts/extract-conflicts.js` (read-only utility to filter the audit CSV down to conflict rows). Fresh `audit-duplicates.js` re-run produced `reports/duplicates-2026-05-16.csv` with 4 conflict rows: PRISM-20 (Create client contract template), PRISM-21 (Run Apollo), PRISM-2 (T-019 close-out), PRISM-213 (T-020). All four resolved `keep_notion` — SQLite-only divergences (priority/due_date) that disappear when T-026/T-027 drop the SQLite tables. Phase 3 gate: 4 reconciliation decisions logged in `reports/reconciliation-log-2026-05-16.jsonl`. Script supports all four resolutions (`keep_notion` → log-only no-op; `keep_sqlite` → push SQLite values into Notion via `notion.pages.update`; `merge_manual` → skip; `archive_both` → archive Notion). |
| T-030 | Sync leverage briefs (`PRISM-Vault/Admin/Leverage-Briefs/T-###.md`) to matching Notion ticket page bodies (one-way: filesystem → Notion). Idempotent. New `scripts/sync-briefs.js` with minimal markdown → Notion-blocks converter (headings, lists, code, tables, quotes, dividers, inline bold/code/links). SHA256 hash in a top marker block lets re-runs skip unchanged briefs. Notion ticket page body becomes display-only. | 2026-05-11 | Merged via PR #16 as `6303c907` (squash). Closed in TASKS.md 2026-05-16 — close-out delayed 5 days, bundled into T-021/T-025's commit per WORKFLOW.md §4. Local branch `task/sync-briefs-to-notion` can be deleted after this PR merges. |
| T-028 | Homepage v2 — Stream D — `adoption_stage` on `clients` + `/api/leads` | 2026-05-08 | Merged via PR #15 as `757f6eb` (squash). Closed-out bundled into T-030's first commit per WORKFLOW.md §4. Adds optional `adoption_stage TEXT` column on the `clients` table via `migrateCRMColumns()`, plus extraction + allow-list validation (`exploring`/`assess`/`architect`/`operate`/`compliance`) in `POST /api/leads`. Newline-escape on `console.warn` for log-injection safety (caught by `security-reviewer`). Pairs with website PR #1's contact form. Leverage brief: `PRISM-Vault/Admin/Leverage-Briefs/T-028.md`. |
| T-024 | Notion-as-source migration Phase 2 — duplicate audit CSV (Phase 2 of 5). | 2026-05-08 | Merged via PR #14 as `f98158e` (squash). Closed-out bundled into T-028's first commit per WORKFLOW.md §4 (delayed from T-024's own merge). The audit script (`scripts/audit-duplicates.js`) pulls all SQLite tickets + action_items, all Notion pages, and emits `reports/duplicates.csv` with `sqlite_id`, `notion_page_id`, `action_item_id`, `prism_id`, `sqlite_due`, `notion_due`, `conflict_fields`, `suggested_winner`. Read-only against prod data; Phase 3 (T-025) gates on Michele's annotated `resolution` column (`keep_notion` / `keep_sqlite` / `merge_manual`). |
| T-023 | Notion-as-source migration Phase 1 — read-path adapter behind `USE_NOTION_SOURCE` feature flag (Phase 1 of 5). | 2026-05-03 | Merged via PR #13 as `3e16405` (squash). Adds `services/notionAdapter.js` with `listTickets` / `listActionItems` / `getTicket` / `summary` against the Notion `Prism AI Tickets` DB; 60-sec in-memory cache with single-flight coalescing (Notion ~3 req/sec ceiling). Three GET routes (`/api/tickets`, `/api/tickets/summary`, `/api/actions`) gated behind per-request `USE_NOTION_SOURCE` env check, default off — SQLite stays authoritative until cutover. On Notion error: `X-Source: sqlite-fallback` header + falls through to SQLite path (Phase 1 safety net; Phase 4 removes). 22 contract tests pass via `npm test` (uses `node:test`, no Jest dependency added). New `scripts/inspect-notion-schema.js` re-runnable read-only inspector. **Phase 1 gate cleared** 2026-05-03 PM ET on dabe-staging: `x-source: notion` header confirmed on all three routes; Notion path returned 218 tickets / 24 fields, summary 8 keys / 149 open, actions 10 items / 10 fields — byte-perfect field-set parity vs prod's SQLite path. Staging service required: (1) push main → staging branch, (2) add `NOTION_API_KEY` + `NOTION_TICKETS_DB_ID` + `USE_NOTION_SOURCE=true` to dabe-staging Variables, (3) explicit Deploy click — Railway didn't auto-redeploy on env-var add. Followups: T-024..T-027 (Phases 2–5). **Lesson recorded** — staging needs the same Notion env vars as prod for the adapter to function; without `NOTION_API_KEY` the adapter throws and silently falls back to SQLite (test surface diverges from intent). |
| PS-053 | Add Prism Studio section to CRM Dashboard (stub — Today / Live Activity / Daily Routine cards + Notion page-content read pattern). Tracks PRISM-122 in Notion. | 2026-04-30 | Merged via PR #12 as `3e6af23` (squash). Adds ★ Prism Studio nav under Operations with three cards. New `services/prismStudioActivityLog.js` establishes a sibling pattern to `services/notionSync.js` for Notion **page-content** reads (vs. T-019's database-row sync). Backend-proxy pattern via `NOTION_API_KEY` chosen over `window.cowork.callMcpTool()` because the latter is a Cowork host primitive not present in the deployed dashboard. Documented in WORKFLOW.md §4.6 under "Notion page-content reads (PS-053)". Both stub-state behaviors resolved post-merge: PS-050 page shared with the dashboard integration so `/activity-log` now returns 10 real blocks; `/today` zero-counts persist until T-021 cleanup lifts `NOTION_SYNC_SINCE=2026-04-30`. Close-out delayed 3 days because session ended without bundling into a next-task housekeeping commit; close-out bundled into T-023's first commit per WORKFLOW.md §4. |
| T-019 | Build Notion → CRM Ticket Sync (Notion `Prism AI Tickets` DB → dashboard `tickets`, polling every 5 min). Tracks PRISM-2 in Notion. | 2026-04-30 | Merged via PR #11 as `429db4a` (squash). Added `services/notionSync.js` plus `POST /api/sync/notion` manual trigger and a 5-minute interval scheduler. Idempotent; matches by `tickets.notion_page_id`; skips Notion pages that have `T-ID` set (T-015's territory). Cutoff `NOTION_SYNC_SINCE=2026-04-30` set in Railway prod env to keep 194 legacy Notion pages out of the dashboard until T-021 cleanup runs. CRM Tickets page now treats any ticket with `notion_page_id` or `task-md`/`src:notion` tag as view-only with "Open in Notion ↗" CTA. End-to-end verified on prod: `0 created · 0 updated · 8 skipped · 0 errors · 347ms`. 3 orphan delivery tickets (TKT-0001..TKT-0003) backfilled into Notion as a one-time migration. 46 `dev-insight` rows hidden from Tickets page (T-020 will surface them). Followups: T-020 (Dev Insights page), T-021 (Notion DB cleanup pre-cutoff-removal). Documented in WORKFLOW.md §4.6. |
| T-018 | Triage and clean up stale untracked files in dashboard repo | 2026-04-28 | Merged via PR #10 as `077923b` (squash). Tracked into the repo: operational scripts (backup, schema diff, snapshot inspect, test portal client), handoff docs + Postman collection, auth pages + 18 marketing pages served by Express static, Netlify build pipeline source. Updated `.gitignore` to exclude `.claude`, `dist`, `.netlify`, `reports`, `*.bak`. Pre-deploy backup workflow (T-007) ran green on the merge commit, confirming push-to-main went through PR-only branch protection (T-004). |
| T-015 | Build TASKS.md ↔ Dashboard ↔ Notion sync (one-way, idempotent) | 2026-04-28 | Merged via PR #9 as `7a0b12f` (squash). Adds `scripts/sync-tasks.js` — parses TASKS.md, writes to dashboard `tickets` table (`category=engineering`, tags `task-md,T-###,src:manual`) and Notion "Prism AI Tickets" DB matched by `T-ID`. Idempotent: re-running with no changes reports `0 created · 0 updated · 16 skipped`. Cross-links: dashboard `notion_page_id` ← Notion page UUID; Notion `Dashboard Ticket ID` ← dashboard `ticket_key`. Required env loaded from `.env`: `NOTION_API_KEY`, `NOTION_TICKETS_DB_ID`, `API_KEY`. Documented in WORKFLOW.md §4.6. Followups filed as T-016 (pre-commit hook) and T-017 (GitHub Action drift detector). |
| T-012 | Re-run T-009 restore against volume-backed dabe DB | 2026-04-28 | **Closed by verification (no implementation merge for T-012's goal).** Branch `task/restore-dabe-from-snapshot` was merged as PR #8 / squash `41b90d1` on 2026-04-27 — but that PR contained only housekeeping (closing T-011a + filing T-013), no restore code. T-012's actual goal (T-009 restore data living on the volume-backed dabe DB) was verified independently on 2026-04-28: prod row counts show `tools=133`, `services=5` — exact matches for the T-009 backup baseline (see T-009 row, T-010 row "Wipe #3"). Conclusion: T-011a's volume attach (`9638aa3`) plus subsequent re-population (likely a sync from the legacy 7058a source after the volume came online) implicitly satisfied this task. No further code change required. |
| T-011a | Attach prod volume + harden `isVolumeReady()` against ephemeral writable dirs | 2026-04-27 | Merged via PR #7 as `9638aa3` (squash). Closes wipe #3 root cause from T-010. **Volume attached:** `dashboard-api-volume` (5 GB) at `/app/data` on `dashboard-api` in production env. **Env vars set:** `DB_PATH=/app/data/prism.db`, `RAILWAY_VOLUME_MOUNT_PATH=/app/data` (Railway auto-injects), `RAILWAY_VOLUME_NAME=dashboard-api-volume`. **Code change:** `isVolumeReady()` now requires `RAILWAY_VOLUME_MOUNT_PATH` to be set AND match expected mount path on Railway; write-test stays as secondary check. **Post-merge verification:** `/health` 200 with new uptime; boot log shows `Mounting volume on: /var/lib/containers/railwayapp/bind-mounts/.../vol_8krewdcg4uie08fi` (real mount, not ephemeral); GitHub Action `Pre-deploy backup` run id `25002998605` succeeded in 13s, uploaded artifact `pre-deploy-backup-9638aa3c09b4b027abbbdbd4399a7cc18e7ab3bd` (34,918 bytes, 90-day retention) — round-trip read+write against the volume-backed DB confirmed. INCIDENT_FINDINGS.md updated with "Wipe #3" section documenting the actual root cause (write-test heuristic could not distinguish ephemeral writable dir from mounted volume; the original "8/8 done" punch list was incorrect — should have been 7/8 with the volume mount step still open). |
| T-010 | Diagnose dabe storage state (read-only) | 2026-04-27 | **No merge SHA — pure investigation.** Confirmed wipe #3 root cause: T-001's fail-hard guard relies on `isVolumeReady()` which only does a write-test, but the Dockerfile's `RUN mkdir -p /app/data` (build log line "stage-1 16/16") creates a writable directory at build time running as root, so the guard passes silently against ephemeral storage. Production has **no volume attached** (`railway volume list` in env=production: empty). NODE_ENV=production ✓, DB_PATH unset, ADMIN_KEY+API_KEY set. Boot log on current container: `Volume /app/data is ready (attempt 1)` then `Opening database at: /app/data/prism.db` — that path is ephemeral. Container uptime ~20h, build image timestamp `2026-04-26T18:03:56Z` confirms wipe-causing redeploy was the T-007 GitHub Action merge. Backup file `Admin/DB-Backups/prism-dabe-post-restore-20260426-104937.db` (516 KB) verified intact: 256 rows total (industries 8, lead_sources 5, team_members 2, services 5, tools 133, business_assets 54, action_items 34, users 2, tickets 13). See INCIDENT_FINDINGS.md "Wipe #3" section. |
| T-008 | Spin up a staging Railway service from a `staging` branch | 2026-04-26 | Merged via PR #6 as `3736338` (squash). Created `dabe-staging` Railway service with own volume `dabe-staging-volume` mounted at `/app/data` (5 GB), deploys from `staging` branch. Used to validate T-009 restore script before prod run. |
| T-000 | Stand up the council, produce WORKFLOW + RUNBOOK + INCIDENT FINDINGS + TASKS | 2026-04-24 | Cowork session output. Files live at repo root. |
| T-001 | Close silent fallback in `getDBPath()` — crash-loud on missing volume in production | 2026-04-24 | Merged via PR #1 as `110dc3c`. Squash commit on top of `e8ac718`. Prod `/health` returned 200 post-deploy; DB accessible. Also added `backups/` to `.gitignore`. |
| T-009 | Restore selected tables from `prism-7058a-*.db` into dabe | 2026-04-26 | Merged via PR #2 as `d29f58e` (squash). 263 of 339 planned rows restored to dabe: industries (8), lead_sources (5), team_members (2), services (5), tools (133), business_assets (54), action_items (34), users (2), tickets (13). 76 rows intentionally NOT restored: projects/invoices/time_entries/payments — all FK-bound to clients (NOT NULL constraint), and policy is "no seed clients in prod / 0 real clients yet ⇒ 0 invoices/etc." Post-restore backup at `backups/prism-dabe-post-restore-20260426-104937.db` (516 KB). |
| T-002 | Take first real backup of prod `prism.db` and store off-disk | 2026-04-26 | 5 .db backups copied from `Development/dashboard/backups/` to `Admin/DB-Backups/` (workspace-root, in Obsidian vault — auto-syncs to cloud if vault sync is on). Files: prism-7058a × 2 (legacy source), prism-dabe × 2 (pre-T-001 + pre-T-009), prism-dabe-post-restore (current dabe state, 516 KB). |
| T-003 | Move AI-coordination protocol docs into dashboard repo (single source of truth) | 2026-04-26 | Merged via PR #3 as `d585608` (squash). Moved WORKFLOW + INCIDENT_FINDINGS + TASKS + DEPLOY_RUNBOOK + PR_JOB_AID into `Development/dashboard/`. All cross-references fixed for sibling layout. Workspace-root CLAUDE.md gained a redirect pointer. |
| T-006 | Update dashboard CLAUDE.md to point at WORKFLOW.md | 2026-04-26 | Rolled into T-003. CLAUDE.md gained a "Read these before any work" section pointing at the 5 protocol docs in priority order. |
| T-004 | Enable GitHub branch protection on `main` | 2026-04-26 | Configured via GitHub Rulesets at `PrismAIAnalytics/prism-ai-dashboard/settings/rules`. Enforces: PR required (0 approvals), block force pushes, restrict deletions, linear history. Bypass list empty (applies to admins too). Verified — direct `git push origin main` rejected with `GH013: Changes must be made through a pull request`. No merge SHA — pure GitHub UI configuration. |
| T-005 | Decide: keep Railway auto-deploy vs switch to manual | 2026-04-26 | **Decision: keep auto-deploy.** With T-001 (volume + fail-hard) and T-004 (branch-protected main, PR-only) in place, the failure modes that wiped prod twice are closed. Remaining risk is "merge a bad PR" — addressed by self-review checklist. No implementation work; decision-only. |
| T-007 | GitHub Action: snapshot prod via `/api/admin/backup-db` on every push to main | 2026-04-26 | Merged via PR #5 as `15b3e57` (squash). After merging, added `DABE_ADMIN_KEY` repo secret + enabled Railway "Wait for CI" toggle on dabe Source settings. First successful run uploaded `prism-dabe-pre-deploy-20260426-174559.db` as an Actions artifact (90-day retention). All future merges to main now auto-snapshot prod before Railway deploys. |

---

## How to use this file

1. **Starting a task:** the AI claims it on its feature branch's **first commit**, which simultaneously (a) closes the previously-merged task with its SHA and (b) moves the new task into In Progress. See [WORKFLOW.md](WORKFLOW.md) §4 "Closing a task — bundle into the next task's first commit."
2. **Finishing a task:** verify the four "done" criteria from [WORKFLOW.md](WORKFLOW.md) §1. The TASKS.md edit (move the row to Done This Week with a `Closed` date and 1-line note including the merge SHA) is bundled into the *next* task's branch — see #1.
3. **Blocked:** move to "Blocked" with a clear blocker. Don't leave it in In Progress — that holds the lock.
4. **Adding new tasks:** append to "Up Next" with a new T-### ID. Keep titles short; one-line "why it matters" forces clarity.
5. **AI sessions:** before doing anything, read this file. If In Progress is non-empty and the owner isn't you, **stop and ask Michele.** (Note: an "In Progress" row may briefly reflect a just-merged task whose close-out is pending — that's the lock being held until the next dispatch picks it up.)

---

## Conventions

- **Task IDs** are sequential `T-###`. Don't reuse numbers.
- **Branches** match the task: `task/short-slug`, `fix/short-slug`, or `chore/short-slug`.
- **One owner per row.** No "Michele + Cowork" — split the task or pick one.
- **Dates** are `YYYY-MM-DD HH:MM` in your local timezone.
- **WIP = 1.** If you find yourself wanting two rows in In Progress, the second one becomes a new top entry in Up Next.
