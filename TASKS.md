# TASKS.md

> **Single source of truth for what's in flight.** This file is the AI-coordination lock — see [WORKFLOW.md](WORKFLOW.md) Section 3.
> **The "In Progress" table contains zero or one row. Never two.**

---

## In Progress

| Task ID | Title | Branch | Owner | Started |
|---------|-------|--------|-------|---------|
| T-024 | Notion-as-source migration Phase 2 — duplicate audit CSV. Pulls SQLite + Notion side-by-side, emits `reports/duplicates.csv` for Michele to annotate. Read-only against prod data; the annotated CSV gates Phase 3. | `task/notion-source-phase2-audit` | Claude Code | 2026-05-03 14:53 |

---

## Up Next (priority order)

| ID | Title | Why it matters | Owner candidate |
|----|-------|----------------|-----------------|
| T-022 | Activate PRISM-3 — Stripe + QuickBooks live data on Financials page (env vars + QBO OAuth, no merge) | PRISM-3 (Notion) integration code shipped 2026-04-09: 3 service files, 13 endpoints, 4-tab Financials page, 6 charts, OAuth flow, in-memory cache, graceful degradation. Verified intact 2026-05-02 (services/stripeService.js, services/quickbooksService.js, server.js:5745-5961). Outstanding work is operational, not code: (1) set `STRIPE_SECRET_KEY` (sk_live_...) on Railway prod env, (2) set `QBO_CLIENT_ID`, `QBO_CLIENT_SECRET`, `QBO_REDIRECT_URI=https://dashboard-api-production-dabe.up.railway.app/api/qbo/callback`, `QBO_ENVIRONMENT=production` on Railway, (3) confirm Intuit Developer app has the Railway callback URL in production OAuth settings, (4) trigger Railway redeploy (auto on env change), (5) smoke-test `GET /api/stripe/balance` returns real numbers, (6) click "Connect QB" on Financials page, complete Intuit OAuth in browser, verify `/api/qbo/status` returns `connected: true` and `qbo_tokens` row written to SQLite. **No merge SHA — this is env + OAuth only, no code change.** **WIP=1 note:** Filed while PS-053 holds the lock; this work is no-merge so it doesn't conflict with the dashboard-repo branch lock — track it as out-of-band operational change with Closed-date entry on completion (no merge column). Followup overlap: T-013 (full Stripe/QBO secret rotation) supersedes this if rotation runs first; if T-022 runs first, T-013 still needs to rotate the same keys later. Note for future ops: 22 days of dormant Stripe + QBO capability — once activated, the Financials page becomes the single source of truth for revenue, surpassing the legacy split between Stripe dashboard and QBO. | Michele (creds + OAuth) + Claude Code (verification) |
| T-025 | Notion-as-source migration Phase 3 — reconciliation backfill from annotated CSV | Phase 3 of 5-phase plan. `scripts/reconcile.js` reads the T-024 annotated CSV and applies decisions via `notion.pages.update` — writes `Action Item ID` onto Notion rows that absorb a dashboard-only ACT row, creates new Notion pages for SQLite-only rows. Dry-run mode first; idempotent. **Gate:** Spot-check ~5 reconciled rows in Notion before flipping to Phase 4. | Claude Code |
| T-026 | Notion-as-source migration Phase 4 — write-path cutover, flip flag, short-circuit cron | Phase 4 of 5-phase plan. Replace SQLite write handlers in `server.js` (POST/PATCH/DELETE on `/api/tickets` + action_items endpoints) with `notion.pages.create` / `update` / archive calls. Invalidate cache key on each write. Flip `USE_NOTION_SOURCE=true` in Railway prod. Short-circuit (don't delete) the T-019 cron handler — becomes a no-op until Phase 5 removes it entirely. **Gate:** 1-hour soak — Michele creates, edits, and closes one ticket and one action item end-to-end. | Claude Code |
| T-027 | Notion-as-source migration Phase 5 — snapshot-then-remove SQLite tables + cleanup | Phase 5 of 5-phase plan. **Final irreversible step.** After Phase 4 soak passes: (1) snapshot SQLite to Railway volume backup AND verify round-trip restore succeeds on `dabe-staging` (Chloe's Phase 5 hardening callout — staging-restore must pass before prod drop), (2) remove the `tickets` and `action_items` tables on prod via SQLite schema migration, (3) delete cron module + `services/notionSync.js`, (4) remove `USE_NOTION_SOURCE` feature flag. Supersedes T-021 (Notion DB cleanup pre-cutoff-removal) — once we cut over, the `NOTION_SYNC_SINCE` env var goes away as a side effect. **Gate:** Michele's explicit go-ahead post-soak. | Claude Code |
| T-013 | Rotate remaining 5 prod secrets (API_KEY, STRIPE_SECRET_KEY, QBO_CLIENT_ID, QBO_CLIENT_SECRET, ANTHROPIC_API_KEY) | Carryover from 2026-04-26 session. ADMIN_KEY rotated under T-012. Each of the 5 needs an external rotation in its own console (Stripe, Intuit Developer, Anthropic), then Railway env update. QBO pair also requires re-running OAuth flow. Worth deferring to a dedicated fresh session — not urgent, just hygiene. The previous "ticket 9ea1da15-..." reference does not point to a real ticket in any tracker (Notion 404, no match in dashboard tickets/action_items). | Claude Code |
| T-014 | Fix missing Sign Out button when logged in | Discovered 2026-04-28 during post-rotation smoke-test on prod. Logged in as `michele` on the dashboard root URL — sidebar shows all nav links (Overview through Finance) but the `.nav-user` div (`index.html:683`) with the username + Sign Out button never renders, leaving the user with no UI path to log out. Workaround documented today: `localStorage.removeItem('prism_token'); location.reload()` in DevTools. **Likely causes to investigate (in order):** (1) `hideLoginScreen()` at `index.html:4864` sets `#nav-user.style.display = 'flex'` — verify this fires after login and that the element is in the DOM at that point. (2) The `<nav>` may have `overflow:hidden` or a fixed height that clips the user block when the menu is long; check the nav CSS rules and the cascade. (3) `.nav-user` CSS may default-hide via media query or be missing layout rules. Repro: log in as any user at https://dashboard-api-production-dabe.up.railway.app/ (root, not /login.html), inspect the sidebar bottom. Expected: username + Sign Out button visible below "Invoices". | Claude Code |
| T-016 | Add pre-commit hook that runs `node scripts/sync-tasks.js` when TASKS.md is staged | Followup to T-015. Today the sync is manual and easy to forget — protocol updates in WORKFLOW.md §4 step 4 help, but a pre-commit hook makes it automatic and fail-loud. Hook should: detect TASKS.md in staged files; run sync; abort the commit if sync fails. Use husky or a plain `.git/hooks/pre-commit` script (latter is repo-portable). | Claude Code |
| T-017 | GitHub Action: run `node scripts/sync-tasks.js` on push to main (drift detector) | Followup to T-015. After PR merges, run sync from CI to catch drift between TASKS.md and the mirrors (e.g., if someone edits TASKS.md without running the script locally). Action should fail the workflow if it produces any creates/updates after the merge — that means the local sync was skipped, which is itself a workflow violation worth flagging. Needs `NOTION_API_KEY` and `DABE_API_KEY` GitHub secrets. | Claude Code |
| T-020 | Dev Insights review surface (monthly improvement loop) | Carved out of T-019 scope on 2026-04-30. Today the dashboard `tickets` table holds 46 `dev-insight` rows (auto-extracted from Claude Code session transcripts via `dev_insights_import.py`, run last session — count grows monthly). They're not editable tickets in the editorial sense — they're machine-generated friction signals (`buggy_code`, `wrong_approach`, etc.) that drive a continuous-improvement loop: review → adopt preventative measure (skill / hook / process change) or dismiss. T-019 hides them from the Tickets page; T-020 builds the dedicated UI: a Dev Insights page with monthly batch review, group-by friction type, inline disposition editing (`new` / `adopted` / `dismissed` / `duplicate`), free-text "preventative measure taken" field, "Convert to ticket" action that spawns a Notion follow-up ticket, and an archive view for prior months. Deliverable: one new page in `public/index.html`, schema additions for disposition fields, monthly cadence documented in WORKFLOW.md. | Claude Code |
| T-021 | Notion "Prism AI Tickets" DB cleanup (pre-sync legacy triage) | Carved out of T-019 scope on 2026-04-30. The Notion DB currently holds 194 pages accumulated over months: 69 with `T-ID` set (TASKS.md mirror — many stale from old sessions), 67 obvious duplicates of dashboard action_items pushed in by older scripts (e.g., "Build client proposal template" / "Build a client proposal template"), 45 stale-completed pages, and a tail of legitimate Notion-native tickets (PRISM-1, PRISM-3, PRISM-39, PS-### Prism Studio). T-019 ships with an `NOTION_SYNC_SINCE=2026-04-30` cutoff so the legacy 194 stay invisible to the sync. T-021 cleans them up so the cutoff can be removed. Deliverable: (1) run `scripts/notion-tickets-inventory.js` (already in repo) to produce a triage doc grouping pages by duplicate clusters / stale T-IDs / stale-completed / active-by-category; (2) Michele marks each row K (keep) / A (archive) / D (delete); (3) build `scripts/apply-notion-cleanup.js` that reads the marked-up doc and executes the decisions in batch via Notion API; (4) confirm the post-cleanup numbers via `scripts/preview-notion-sync.js`; (5) remove `NOTION_SYNC_SINCE` from Railway prod env. Acceptance: 0 duplicate clusters in inventory, total Notion page count reflects only the legitimate ticket set, dashboard shows all of them after the sync runs without the cutoff. | Michele + Claude Code |

---

## Blocked

_(none yet)_

| ID | Title | Blocked by | Notes |
|----|-------|------------|-------|

---

## Done This Week

| ID | Title | Closed | Notes |
|----|-------|--------|-------|
| T-023 | Notion-as-source migration Phase 1 — read-path adapter behind `USE_NOTION_SOURCE` feature flag (Phase 1 of 5). | 2026-05-03 | Merged via PR #13 as `3e16405` (squash). Adds `services/notionAdapter.js` with `listTickets` / `listActionItems` / `getTicket` / `summary` against the Notion `Prism AI Tickets` DB; 60-sec in-memory cache with single-flight coalescing (Notion ~3 req/sec ceiling). Three GET routes (`/api/tickets`, `/api/tickets/summary`, `/api/actions`) gated behind per-request `USE_NOTION_SOURCE` env check, default off — SQLite stays authoritative until cutover. On Notion error: `X-Source: sqlite-fallback` header + falls through to SQLite path (Phase 1 safety net; Phase 4 removes). 22 contract tests pass via `npm test` (uses `node:test`, no Jest dependency added). New `scripts/inspect-notion-schema.js` re-runnable read-only inspector. **Phase 1 gate cleared** 2026-05-03 PM ET on dabe-staging: `x-source: notion` header confirmed on all three routes; Notion path returned 218 tickets / 24 fields, summary 8 keys / 149 open, actions 10 items / 10 fields — byte-perfect field-set parity vs prod's SQLite path. Staging service required: (1) push main → staging branch, (2) add `NOTION_API_KEY` + `NOTION_TICKETS_DB_ID` + `USE_NOTION_SOURCE=true` to dabe-staging Variables, (3) explicit Deploy click — Railway didn't auto-redeploy on env-var add. Followups: T-024..T-027 (Phases 2–5). **Lesson recorded** — staging needs the same Notion env vars as prod for the adapter to function; without `NOTION_API_KEY` the adapter throws and silently falls back to SQLite (test surface diverges from intent). |
| PS-053 | Add Prism Studio section to CRM Dashboard (stub — Today / Live Activity / Daily Routine cards + Notion page-content read pattern). Tracks PRISM-122 in Notion. | 2026-04-30 | Merged via PR #12 as `3e6af23` (squash). Adds ★ Prism Studio nav under Operations with three cards. New `services/prismStudioActivityLog.js` establishes a sibling pattern to `services/notionSync.js` for Notion **page-content** reads (vs. T-019's database-row sync). Backend-proxy pattern via `NOTION_API_KEY` chosen over `window.cowork.callMcpTool()` because the latter is a Cowork host primitive not present in the deployed dashboard. Documented in WORKFLOW.md §4.6 under "Notion page-content reads (PS-053)". Both stub-state behaviors resolved post-merge: PS-050 page shared with the dashboard integration so `/activity-log` now returns 10 real blocks; `/today` zero-counts persist until T-021 cleanup lifts `NOTION_SYNC_SINCE=2026-04-30`. Close-out delayed 3 days because session ended without bundling into a next-task housekeeping commit; close-out bundled into T-023's first commit per WORKFLOW.md §4. |
| T-019 | Build Notion → CRM Ticket Sync (Notion `Prism AI Tickets` DB → dashboard `tickets`, polling every 5 min). Tracks PRISM-2 in Notion. | 2026-04-30 | Merged via PR #11 as `429db4a` (squash). Added `services/notionSync.js` plus `POST /api/sync/notion` manual trigger and a 5-minute interval scheduler. Idempotent; matches by `tickets.notion_page_id`; skips Notion pages that have `T-ID` set (T-015's territory). Cutoff `NOTION_SYNC_SINCE=2026-04-30` set in Railway prod env to keep 194 legacy Notion pages out of the dashboard until T-021 cleanup runs. CRM Tickets page now treats any ticket with `notion_page_id` or `task-md`/`src:notion` tag as view-only with "Open in Notion ↗" CTA. End-to-end verified on prod: `0 created · 0 updated · 8 skipped · 0 errors · 347ms`. 3 orphan delivery tickets (TKT-0001..TKT-0003) backfilled into Notion as a one-time migration. 46 `dev-insight` rows hidden from Tickets page (T-020 will surface them). Followups: T-020 (Dev Insights page), T-021 (Notion DB cleanup pre-cutoff-removal). Documented in WORKFLOW.md §4.6. |
| T-018 | Triage and clean up stale untracked files in dashboard repo | 2026-04-28 | Merged via PR #10 as `077923b` (squash). Tracked into the repo: operational scripts (backup, schema diff, snapshot inspect, test portal client), handoff docs + Postman collection, auth pages + 18 marketing pages served by Express static, Netlify build pipeline source. Updated `.gitignore` to exclude `.claude`, `dist`, `.netlify`, `reports`, `*.bak`. Pre-deploy backup workflow (T-007) ran green on the merge commit, confirming push-to-main went through PR-only branch protection (T-004). |
| T-015 | Build TASKS.md ↔ Dashboard ↔ Notion sync (one-way, idempotent) | 2026-04-28 | Merged via PR #9 as `7a0b12f` (squash). Adds `scripts/sync-tasks.js` — parses TASKS.md, writes to dashboard `tickets` table (`category=engineering`, tags `task-md,T-###,src:manual`) and Notion "Prism AI Tickets" DB matched by `T-ID`. Idempotent: re-running with no changes reports `0 created · 0 updated · 16 skipped`. Cross-links: dashboard `notion_page_id` ← Notion page UUID; Notion `Dashboard Ticket ID` ← dashboard `ticket_key`. Required env loaded from `.env`: `NOTION_API_KEY`, `NOTION_TICKETS_DB_ID`, `API_KEY`. Documented in WORKFLOW.md §4.6. Followups filed as T-016 (pre-commit hook) and T-017 (GitHub Action drift detector). |
| T-012 | Re-run T-009 restore against volume-backed dabe DB | 2026-04-28 | **Closed by verification (no implementation merge for T-012's goal).** Branch `task/restore-dabe-from-snapshot` was merged as PR #8 / squash `41b90d1` on 2026-04-27 — but that PR contained only housekeeping (closing T-011a + filing T-013), no restore code. T-012's actual goal (T-009 restore data living on the volume-backed dabe DB) was verified independently on 2026-04-28: prod row counts show `tools=133`, `services=5` — exact matches for the T-009 backup baseline (see T-009 row, T-010 row "Wipe #3"). Conclusion: T-011a's volume attach (`9638aa3`) plus subsequent re-population (likely a sync from the legacy 7058a source after the volume came online) implicitly satisfied this task. No further code change required. |
| T-011a | Attach prod volume + harden `isVolumeReady()` against ephemeral writable dirs | 2026-04-27 | Merged via PR #7 as `9638aa3` (squash). Closes wipe #3 root cause from T-010. **Volume attached:** `dashboard-api-volume` (5 GB) at `/app/data` on `dashboard-api` in production env. **Env vars set:** `DB_PATH=/app/data/prism.db`, `RAILWAY_VOLUME_MOUNT_PATH=/app/data` (Railway auto-injects), `RAILWAY_VOLUME_NAME=dashboard-api-volume`. **Code change:** `isVolumeReady()` now requires `RAILWAY_VOLUME_MOUNT_PATH` to be set AND match expected mount path on Railway; write-test stays as secondary check. **Post-merge verification:** `/health` 200 with new uptime; boot log shows `Mounting volume on: /var/lib/containers/railwayapp/bind-mounts/.../vol_8krewdcg4uie08fi` (real mount, not ephemeral); GitHub Action `Pre-deploy backup` run id `25002998605` succeeded in 13s, uploaded artifact `pre-deploy-backup-9638aa3c09b4b027abbbdbd4399a7cc18e7ab3bd` (34,918 bytes, 90-day retention) — round-trip read+write against the volume-backed DB confirmed. INCIDENT_FINDINGS.md updated with "Wipe #3" section documenting the actual root cause (write-test heuristic could not distinguish ephemeral writable dir from mounted volume; the original "8/8 done" punch list was incorrect — should have been 7/8 with the volume mount step still open). |
| T-010 | Diagnose dabe storage state (read-only) | 2026-04-27 | **No merge SHA — pure investigation.** Confirmed wipe #3 root cause: T-001's fail-hard guard relies on `isVolumeReady()` which only does a write-test, but the Dockerfile's `RUN mkdir -p /app/data` (build log line "stage-1 16/16") creates a writable directory at build time running as root, so the guard passes silently against ephemeral storage. Production has **no volume attached** (`railway volume list` in env=production: empty). NODE_ENV=production ✓, DB_PATH unset, ADMIN_KEY+API_KEY set. Boot log on current container: `Volume /app/data is ready (attempt 1)` then `Opening database at: /app/data/prism.db` — that path is ephemeral. Container uptime ~20h, build image timestamp `2026-04-26T18:03:56Z` confirms wipe-causing redeploy was the T-007 GitHub Action merge. Backup file `Admin/DB-Backups/prism-dabe-post-restore-20260426-104937.db` (516 KB) verified intact: 256 rows total (industries 8, lead_sources 5, team_members 2, services 5, tools 133, business_assets 54, action_items 34, users 2, tickets 13). See INCIDENT_FINDINGS.md "Wipe #3" section. |
| T-008 | Spin up a staging Railway service from a `staging` branch | 2026-04-26 | Merged via PR #6 as `3736338` (squash). Created `dabe-staging` Railway service with own volume `dabe-staging-volume` mounted at `/app/data` (5 GB), deploys from `staging` branch. Used to validate T-009 restore script before prod run. |
| T-000 | Stand up the council, produce WORKFLOW + RUNBOOK + INCIDENT FINDINGS + TASKS | 2026-04-24 | Cowork session output. Files live at repo root. |
| T-001 | Close silent fallback in `getDBPath()` — crash-loud on missing volume in production | 2026-04-24 | Merged via PR #1 as `110dc3c`. Squash commit on top of `e8ac718`. Prod `/health` returned 200 post-deploy; DB accessible. Also added `backups/` to `.gitignore`. |
| T-009 | Restore selected tables from `prism-7058a-*.db` into dabe | 2026-04-26 | Merged via PR #2 as `d29f58e` (squash). 263 of 339 planned rows restored to dabe: industries (8), lead_sources (5), team_members (2), services (5), tools (133), business_assets (54), action_items (34), users (2), tickets (13). 76 rows intentionally NOT restored: projects/invoices/time_entries/payments — all FK-bound to clients (NOT NULL constraint), and policy is "no seed clients in prod / 0 real clients yet ⇒ 0 invoices/etc." Post-restore backup at `backups/prism-dabe-post-restore-20260426-104937.db` (516 KB). |
| T-002 | Take first real backup of prod `prism.db` and store off-disk | 2026-04-26 | 5 .db backups copied from `Development/dashboard/backups/` to `Admin/DB-Backups/` (workspace-root, in Obsidian vault — auto-syncs to cloud if vault sync is on). Files: prism-7058a × 2 (legacy source), prism-dabe × 2 (pre-T-001 + pre-T-009), prism-dabe-post-restore (current dabe state, 516 KB). |
| T-003 | Move AI-coordination protocol docs into dashboard repo (single source of truth) | 2026-04-26 | Merged via PR #3 as `d585608` (squash). Moved WORKFLOW + INCIDENT_FINDINGS + TASKS + DEPLOY_RUNBOOK + PR_JOB_AID into `Development/dashboard/`. All cross-references fixed for sibling layout. Workspace-root CLAUDE.md gained a redirect pointer. |
| T-006 | Update dashboard CLAUDE.md to point at WORKFLOW.md | 2026-04-26 | Rolled into T-003. CLAUDE.md gained a "Read these before any work" section pointing at the 5 protocol docs in priority order. |
| T-004 | Enable GitHub branch protection on `main` | 2026-04-26 | Configured via GitHub Rulesets at `PrismAIAnalytics/prism-ai-dashboard/settings/rules`. Enforces: PR required (0 approvals), block force pushes, restrict deletions, linear history. Bypass list empty (applies to admins too). Verified — direct `git push origin main` rejected with `GH013: Changes must be made through a pull request`. No merge SHA — pure GitHub UI configuration. |
| T-005 | Decide: keep Railway auto-deploy vs switch to manual | 2026-04-26 | **Decision: keep auto-deploy.** With T-001 (volume + fail-hard) and T-004 (branch-protected main, PR-only) in place, the failure modes that wiped prod twice are closed. Remaining risk is "merge a bad PR" — addressed by self-review checklist. No implementation work; decision-only. |
| T-007 | GitHub Action: snapshot prod via `/api/admin/backup-db` on every push to main | 2026-04-26 | Merged via PR #5 as `15b3e57` (squash). After merging, added `DABE_ADMIN_KEY` repo secret + enabled Railway "Wait for CI" toggle on dabe Source settings. First successful run uploaded `prism-dabe-pre-deploy-20260426-174559.db` as an Actions artifact (90-day retention). All future merges to main now auto-snapshot prod before Railway deploys. |

---

## How to use this file

1. **Starting a task:** the AI claims it on its feature branch's **first commit**, which simultaneously (a) closes the previously-merged task with its SHA and (b) moves the new task into In Progress. See [WORKFLOW.md](WORKFLOW.md) §4 "Closing a task — bundle into the next task's first commit."
2. **Finishing a task:** verify the four "done" criteria from [WORKFLOW.md](WORKFLOW.md) §1. The TASKS.md edit (move the row to Done This Week with a `Closed` date and 1-line note including the merge SHA) is bundled into the *next* task's branch — see #1.
3. **Blocked:** move to "Blocked" with a clear blocker. Don't leave it in In Progress — that holds the lock.
4. **Adding new tasks:** append to "Up Next" with a new T-### ID. Keep titles short; one-line "why it matters" forces clarity.
5. **AI sessions:** before doing anything, read this file. If In Progress is non-empty and the owner isn't you, **stop and ask Michele.** (Note: an "In Progress" row may briefly reflect a just-merged task whose close-out is pending — that's the lock being held until the next dispatch picks it up.)

---

## Conventions

- **Task IDs** are sequential `T-###`. Don't reuse numbers.
- **Branches** match the task: `task/short-slug`, `fix/short-slug`, or `chore/short-slug`.
- **One owner per row.** No "Michele + Cowork" — split the task or pick one.
- **Dates** are `YYYY-MM-DD HH:MM` in your local timezone.
- **WIP = 1.** If you find yourself wanting two rows in In Progress, the second one becomes a new top entry in Up Next.
